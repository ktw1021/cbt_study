# 개선 기록 — 2026-09-16

패치 `patch-notes-2026-09-16.md`와 한 쌍.
형식: **문제 / 원인 / 구체적 수정**.

---

## 1. 원문에 굵게·색·형광·정렬이 없고 학습에 안 보임 — DONE

- **문제**
  - 긴 문장에 강조·색·문단 정렬을 남길 수 없고, 학습 화면에도 안 보인다.
  - 글자만 되돌리면 서식·각주·빈칸이 따로 논다. 정렬 후 줄이 늘거나, 다줄 정답이 동의어로 갈라질 수 있다.
- **원인**
  - 평문만 저장하고 렌더러가 서식을 모른다. 정렬용 블록을 감싼 뒤 offset을 다시 세면 줄이 는다.
  - `Range.toString()`은 블록 줄바꿈을 빼먹는다. 불러오기가 줄바꿈을 `||`처럼 추측한다.
- **구체적 수정**
  - 가시 원문 UTF-16 위치(`textMarks`)와 평문을 분리. 문제·해설 공용 렌더러.
  - 원문·칩을 먼저 읽고 한 번에 그린다. 빈칸 정답은 가시 원문+선택 offset.
  - 문단 정렬, 색·형광·볼드. 텍스트·서식·각주·빈칸 메타를 한 Undo.
  - 저장·가져오기에 서식 포함. 구버전의 명시적 `||`만 동의어로 이관. 학습에서도 동일 렌더, `ui.studyFontScale` persist.
  - IME는 키 플래그만이 아니라 조합 시작·끝도 추적해 조합 중 화살표를 가로채지 않는다.
  - 파일: `js/domain/text-marks.js`, `js/ui/editor-surface.js`, `js/ui/chip-editor.js`, `js/ui/mark-render.js`, `js/utils/text.js`.

---

## 2. 본문 위치에 각주를 달고 제작·학습에서 같이 보기 어려움 — DONE

- **문제**
  - 원하는 자리에 각주를 붙이고, 제작·집중 편집·학습에서 같은 목록으로 오갈 수 없다.
  - 가져온 카드의 각주 수정이 따옴표 등 id에서 깨질 수 있다.
- **원인**
  - 카드 메모만 있고 앵커가 없다. 번호를 id·저장 순으로 매기면 본문 순서와 어긋난다.
  - 각주 id를 CSS 선택자에 넣으면 특수문자에서 실패한다.
- **구체적 수정**
  - 커서·선택 범위가 유효할 때만 삽입. 번호는 필드별 실제 표시 위치(`end`) 순. 각주를 원문 길이에 넣지 않는다.
  - 편집 불가 번호 양옆의 중복 커서 정지를 합치고, 키보드 삭제도 확인한다.
  - 제작·집중 편집·학습 공용 패널. 제목은 본문 이동, 화살표는 접기. hover 내용, 본문↔목록 일시 강조.
  - 각주 내용이 다른 카드는 폴더 가져오기에서 중복으로 버리지 않는다(`semanticFingerprint`). 수정은 id를 데이터로 비교.
  - 파일: `js/ui/notes-panel.js`, `js/domain/text-marks.js`, `js/services/import-export.js`.

---

## 3. 학습 중 수정이 이중이고 학습 복귀가 약함 — DONE

- **문제**
  - 학습 화면 아래 인플레이스 수정과 오른쪽 위 **수정**이 겹친다.
  - 제작에서 빈칸을 바꾼 뒤 돌아오면 이동 목록·채점이 옛 카드에 남을 수 있다.
- **원인**
  - `#studyEditPanel`과 `edit-study-card`가 같은 일을 한다. 학습 상태는 빈칸 순서만 보고 id 재연결을 안 한다.
- **구체적 수정**
  - 인플레이스 폼 제거. 플레이바 **수정**만 남기고 카드 제작으로 보낸다 (`_createStudyReturn`).
  - 진행 중이면 「이어서 학습하기」, 아니면 「학습하러 가기」(그 카드 한 장). 미저장·진행 중 세트 변경은 확인.
  - 빈칸 id로 기존 입력·채점을 잇고, 추가·정답 변경된 칸만 새 상태. 이동 목록도 갱신.
  - 파일: `index.html`, `js/app/actions.js`, `js/ui/create.js`, `js/ui/study.js`.

---

## 4. 빈칸 이동 버튼이 많고 문제 가리기·좁은 화면이 잘림 — DONE

- **문제**
  - 빈칸마다 이동 버튼이 늘고, 문제를 가려도 해설이 남는 폭을 못 쓴다.
  - 좁은 화면에서 세 패널을 쌓으면 첫 행이 거의 사라진다.
- **원인**
  - 칸 수만큼 버튼을 그린다. 가리기가 해설 폭을 안 늘린다. 한 열 grid 높이 제약으로 첫 행이 2px까지 줄어든다.
- **구체적 수정**
  - 「빈칸」 하나 + 펼친 목록에서 점프. 문제 가리기 시 해설이 남는 폭을 쓴다.
  - 좁은 화면: grid 높이 제약 해제, 각 본문에 스크롤 높이.
  - 파일: `js/ui/study-blank-nav.js`, `css/layout.css`, `css/components.css`, `js/ui/study.js`.

---

## 5. 여러 카드를 골라 한꺼번에 옮기기 어려움 — DONE

- **문제**
  - 연속·떨어진 카드를 고른 뒤 기존 드래그로 폴더 이동하려면 한 장씩이어야 했다.
- **원인**
  - 관리 목록이 단일 선택 중심. 드래그가 현재 한 장만 본다.
- **구체적 수정**
  - Shift+클릭 범위, Ctrl+클릭 추가/해제. 선택된 채로 기존 드래그하면 고른 카드 전체를 폴더 이동.
  - 파일: `js/ui/manage-selection.js`, `js/ui/manage.js`, `js/app/actions.js`.

---

## 6. 사이드바에서 로컬 계정을 바로 바꾸기 어렵고, 옛 세션이 섞일 수 있음 — DONE

- **문제**
  - 로그아웃 없이 다른 로컬 계정으로 넘어가기 어렵다.
  - 공용 `studySession`이 있으면 계정 전환 후 남의 학습이 남을 수 있다.
- **원인**
  - 전환 UI가 없다. 구버전 세션이 계정 밖에 있고 런타임에서 재할당될 수 있다.
- **구체적 수정**
  - 사이드바 이름을 누르면 목록 → PIN. 취소 시 현재 계정 유지. 같은 유저가 아니면 카드관리 홈으로 보낸 뒤 런타임을 비운다.
  - 계정별 `studySessions`. 공용 `studySession`은 당시 계정에 한 번만 이관하고 비운다. 복원 카드 소유권 확인.
  - 파일: `js/ui/account-switcher.js`, `js/ui/sidebar.js`, `js/domain/migrate.js`, `js/services/study-session.js`, `js/app/actions.js`.

---

## 잔여 / 알려진 리스크

| 항목 | 상태 |
|------|------|
| 실제 OS 한글 IME·다른 엔진 | 브라우저 테스트는 합성 composition 이벤트. 실기기 IME는 별도. |
| 좁은 학습 화면 | 본문 스크롤은 넣었으나 세 패널을 한 열로 쌓으면 높이가 빠듯하다. |
| 학습 중 인플레이스 수정 | 패널 제거. 플레이바 **수정** → 카드 제작만. |
| 같은 로그인 세션 Undo | 계정 전환 시에만 비움. |

---

## 의도적으로 안 한 것

- 서식·각주용 새 에디터를 만들지 않음. 공용 렌더러·칩 에디터를 쓴다.
- DB 이름·저장 키를 바꾸지 않음.
- 학습 중 인플레이스 수정 폼을 남기지 않음.
- 학습 기본 순서를 랜덤으로 바꾸지 않음.
- 조회 함수 전부에 폴더 순환 가드를 넣지 않음.
