# 개발 기록 — 2026-09-16

기준: `b63c0c7` 이후 변경. 사용자 안내: `patch-notes-2026-09-16.md`.

## 주요 변경

| 영역 | 결정·구현 |
|---|---|
| 서식·각주 | 평문과 가시 원문 UTF-16 위치를 분리해 저장. 문제·해설 모두 공용 렌더러를 사용하고, 각주 번호는 필드별 실제 표시 위치 순서로 계산 |
| 편집기 | 문단 정렬, 색·형광·볼드, 위치가 유효할 때만 각주 삽입. 텍스트·서식·각주·빈칸 메타를 함께 Undo |
| 각주·메모 | 제작·집중 편집·학습의 공용 패널. 제목은 본문 이동, 화살표는 접기. 삭제 확인, hover 내용, 본문↔목록 일시 강조 |
| 학습 | 빈칸 이동 드롭다운, 본문 크기, 문제 가리기 폭 처리. 중복 수정 폼을 정리하고 제작 화면에서 수정 후 학습 복귀 |
| 관리·계정 | 단일·Shift·Ctrl 선택 후 기존 드래그로 폴더 이동. 사용자 이름에서 PIN을 확인해 로컬 계정 전환 |
| 저장·공유 | 서식·각주를 저장·가져오기에 포함. 각주 내용이 다른 카드는 중복으로 제외하지 않음. DB 이름·저장 키 유지 |

## 결함과 수정

- **정렬 후 줄바꿈 증가:** DOM을 일부 감싼 상태에서 위치를 재계산하던 문제. 원문·칩을 먼저 읽고 한 번에 렌더한다.
- **다줄 정답 손실:** 불러오기에서 줄바꿈을 동의어 구분자로 추측하던 처리를 제거했다. 구버전의 명시적 `||`만 이관한다.
- **각주 위치·번호·커서 정지:** 각주를 원문 길이에 포함하지 않고, 실제 표시 끝점으로 정렬한다. 편집 불가 번호 양옆의 중복 커서 정지를 합치고 키보드 삭제도 확인한다.
- **검수 중 발견한 문단→빈칸 손실:** `Range.toString()`은 정렬용 블록의 줄바꿈을 생략한다. 가시 원문과 선택 offset으로 정답을 읽도록 수정했다.
- **가져온 각주 수정 실패:** id를 CSS 선택자에 삽입하던 경로를 데이터 비교로 바꿨다. 따옴표 등이 포함된 id도 수정·저장한다.
- **구버전 학습 진행의 계정 혼입:** 공용 `studySession`을 당시 계정에 한 번만 이관하고 비운다. 런타임에서 재할당하지 않으며 복원할 카드의 소유권도 확인한다.
- **수정 후 학습의 빈칸 불일치:** 빈칸 id로 기존 입력·채점을 연결하고, 추가·정답 변경된 빈칸만 새 상태로 만든다. 이동 목록도 갱신한다.
- **IME 중 화살표 개입:** 키 이벤트의 플래그뿐 아니라 조합 시작·끝도 추적해 조합 중에는 가로채지 않는다.
- **좁은 학습 화면 잘림:** 세 패널을 한 열로 쌓을 때 첫 행이 2px로 줄던 grid 높이 제약을 해제하고 각 본문에 스크롤 높이를 준다.

## 검증

- `node tests/all.mjs`: **10/10 묶음 통과**. 단위 47개와 브라우저 9개 스위트.
- 단위: 서식·각주, 빈칸 복원·학습 상태 재연결, 가져오기, 구버전 계정별 세션.
- 브라우저: 실제 클릭·키보드·드래그 후 원문·메타·저장·학습 상태 확인. 390px 화면과 합성 IME 조합 이벤트도 포함.
- 별도 루프백 호스트: 서비스워커 제어, 버전 변경에 따른 재로딩, 저장 카드 유지, 최신 패치 목록 확인.
- 실제 OS 한글 IME와 다른 브라우저 엔진, 원격 staging·운영 배포는 별도 검수 대상이다.

## 운영

버전 `2026-09-16`. 배포 흐름·문서 갱신 절차는 [RELEASING.md](RELEASING.md), 검사 범위는 [tests/README.md](tests/README.md)에 둔다. 테스트 데이터는 실사용 DB와 분리한다. 런타임 라이브러리·빌드 단계는 추가하지 않았다.
