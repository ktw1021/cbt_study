# 개선 기록 — 2026-06-25

테스터 피드백·당일 작업. 형식: 문제 / 원인 / 해결방향 / 구체적 수정.

---

## 1. 학습 빈칸 호버 힌트 표시 시간

- **문제**
  - 빈칸 호버 정답 힌트가 약 0.5초 후 자동 사라짐.
- **원인**
  - `js/ui/study.js` `PEEK_SHOW_MS` + `setTimeout` 자동 숨김.
- **구체적 수정** (DONE)
  - `PEEK_SHOW_MS`, `peekHideTimer` 제거. `handleBlankPeekOut`만 숨김. 1.5초 게이지 대기 유지.

---

## 2. 901~1100px 구간 본문이 사이드바 아래로 밀림

- **문제**
  - 데스크톱 사이드바 UI인데 `.app`이 1열로 쌓여 본문이 사이드바 아래로 밀림(스크롤 0에서 본문 미표시).
- **원인**
  - `css/layout.css` `@media (max-width: 1100px)`에 `.app` 포함 → `grid-template-columns: 1fr`. 900px 이하 햄버거와 겹치지 않는 **901~1100 데드존**.
- **구체적 수정** (DONE)
  - 1100px MQ에서 `.app` 제외. 내부 `grid2`·`study-layout`·`create-split`·`manage-split`만 1열 유지.

---

## 3. 카드관리 폴더 선택 → 새 카드 제작 시 folderId 미반영

- **문제**
  - `activeFolderId`는 설정되나 `startNewCard()`가 `renderCreateForm(null)`만 호출.
- **구체적 수정** (DONE)
  - `newCardSeedFromContext()` — `store.activeFolderId` → `{ folderId }`.
  - `startNewCard()`: 관리 등에서 진입 시 seed 적용; **이미 `create` 섹션**이면 seed `null`(완전 초기화).
  - `goCreate()`: 초안 없을 때 동일 seed.

---

## 4. data-action 클릭 연타·중복 실행

- **문제**
  - `save-card` 등 async 작업 연타 시 새 카드 중복 생성 가능. 가드 없음.
- **구체적 수정** (DONE)
  - `js/utils/action-guard.js`: `runGuardedClick`, `actionGuardKey`. 동일 키 in-flight 중 재실행 무시.
  - `js/main.js`: 모든 `data-action` 클릭·`Ctrl+S` 저장에 적용.
  - 폴더/카드/플래그 등은 `TARGET_SCOPED`로 키에 id 분리 → 빠른 화면 전환은 유지.

---

## 5. 카드제작 빈칸 전체 해제

- **구체적 수정** (DONE)
  - `js/ui/chip-editor.js`: `chipBlankCount`, `chipRemoveAll`.
  - `js/app/actions.js`: `removeAllBlanks` (confirm 후 일괄 해제).
  - `index.html` 툴바 「빈칸 전체 해제」, `main.js` `remove-all-blanks`.

---

## 6. 카드관리 UI

- **구체적 수정** (DONE)
  - `js/ui/manage.js`: 빵부스러기 「전체」 — `카드 N개 · 미분류 M개` (`countUnclassifiedCards`).
  - `js/domain/queries.js`: `countUnclassifiedCards` (`!folderId`).
  - `css/components.css`: `.manage-split` **5.5fr 4.5fr**. `.tree-meta`, 빵부스러기·트리 `user-select: none`.
  - `index.html`: `manage-split` 클래스, 안내 문구.

---

## 7. 폴더 트리 드래그 이동

- **문제**
  - 폴더 구조 변경은 이름 수정·삭제만 가능, 트리에서 부모 변경 불가.
- **구체적 수정** (DONE)
  - `js/app/actions.js`: `moveFolder(folderId, newParentId)` — `parentId` 갱신, 하위→자손 이동 차단.
  - `js/ui/manage.js`: `tree-left` `draggable` + `data-drag-folder`. 토글 `draggable="false"`.
  - `js/main.js`: `#folderPanel` — 폴더 행(`[data-folder-id]`) 밖 드롭 → 카드 `folderId: null` 또는 폴더 루트. 빵부스러기·트리 여백 포함.
- **참고**
  - DnD 이벤트(`main.js`)와 이동 액션(`actions.js`)은 카드·폴더 공통 패턴이나 아직 모듈 미분리. 추후 `tree-dnd.js` 등 통합 여지.

---

## 8. 기타

- `AGENT.md` + `.gitignore`에 등록 (로컬 에이전트 원칙, IndexedDB 데이터 주의).

---

## 점검 — 알려진 잔여·참고

| 항목 | 상태 |
|------|------|
| ≤900px 카드제작 **저장** vs 상단 **제작** 버튼 혼동 | **미착수** — 저장 버튼이 스크롤 밖, topbar 「제작」은 `goCreate()`(저장 아님). sticky 저장 또는 topbar 동작 변경 검토. |
| 901~1100px 본문 폭 | 레이아웃 붕괴는 수정됨. 사이드바 300px + 본문 ~600px로 다소 좁을 수 있음. 향후 ≤1100 햄버거 통일은 별도. |
| `goCreate()` + 초안 없음 + 이미 create 화면 | 사이드바 「카드제작」 재클릭 시 `activeFolderId` seed 재적용 가능 — 빈도 낮음. |
| `app.bundle.js` | 소스는 `main.js` 모듈. 배포가 bundle 쓰면 `npm run build` 필요(현재 index는 `main.js` 직접 로드). |
| IndexedDB·스키마 | 당일 변경 없음. 저장 키·필드명 유지. |
| 폴더 DnD 모듈화 | 카드·폴더 drop 로직 동형 — `main.js`에 공존, 리팩터는 미착수. |
