# 개선 기록 — 2026-09-04

패치 `patch-notes-2026-09-04.md`와 한 쌍.
형식: **문제 / 원인 / 구체적 수정**.

---

## 1. 홈 버튼이 없음 — DONE

- **문제**
  - 어디서든 「홈」(카드관리)으로 돌아갈 명확한 진입점이 약함.
- **원인**
  - 타이틀이 장식 텍스트에 가깝고, 네비 액션이 붙어 있지 않았음.
- **구체적 수정**
  - 사이드바·상단바 타이틀 → `data-action="nav-manage"`.
  - `.app-home-btn`로 전역 `button` 스타일만 되돌림. 별도 라우트 로직 없음.

---

## 2. 폴더 단위 카드 공유가 없음 — DONE

- **문제**
  - 스터디원이 만든 폴더·카드를 파일로 주고받아 **내 계정**에 넣는 경로가 필요함.
- **원인**
  - 기존 내보내기가 계정 이전(`user`/`all`) 중심. 폴더만 골라 공유하는 타입이 없었음.
- **구체적 수정**
  - `folder` 타입: 고른 폴더+하위 카드, 미분류 제외, 유저·PIN 없음, 학습기록 strip, `author` 유지/채움.
  - 가져오기: 현재 로그인 유저 루트에 새 id. 이름 겹치면 `(1)`.
  - 제목·해설·빈칸 동일 → 중복 목록 후 건너뛰기/가져오기.
  - 파일: `js/services/import-export.js` (`applyFolderImport`, `previewFolderImport` 등).

---

## 3. 계정 이전 시 기존 데이터를 건드리거나 덮어쓸 위험 — DONE

- **문제**
  - 유저/유저들 파일을 옮길 때 받는 쪽 계정을 합치거나 덮어쓰면 안 됨. PIN이 파일에 실리면 안 됨.
- **원인**
  - 예전 경로에 overwrite/merge 뉘앙스가 있었거나, 세션·PIN 처리가 불명확할 수 있음.
- **구체적 수정**
  - `user`/`all`: **추가만**. 새 id. 이름 겹치면 `(1)`.
  - PIN 미포함 → 가져온 직후 `showPinSetup`(복수면 큐).
  - 이미 로그인 중이면 그 세션 유지 (`pinSetupResumeUserId`). 가져온 계정으로 자동 전환하지 않음.
  - 가입 시 동명 거절 (`isUserNameTaken`). `(1)`은 가져오기만.
  - `readImportFile` / `describeImport` / `openChoice`. 앱 JSON 아니면 거절.
  - `persist`: 디스크에 users 있는데 메모리 users 비면 거부. 마지막 계정 삭제만 `allowEmptyUsers`.
  - init: migrate 후 즉시 persist 하지 않음 (빈 상태 덮어쓰기 방지).

---

## 4. 배포 후 강한 새로고침 의존 — DONE

- **문제**
  - GitHub Pages 등에서 JS/CSS 캐시 때문에 새 패치가 안 먹는 경우가 많음.
- **원인**
  - 버전 신호 없이 브라우저·CDN이 예전 자산을 붙잡음.
- **구체적 수정**
  - `version.json` + `sw.js`. 새 `v`면 한 번 리로드.
  - localhost / `[::1]` / `127.0.0.1`에서는 SW 생략.
  - `.nojekyll`: Pages(Jekyll)가 점 파일 배포를 빼먹는 것 방지.

---

## 5. 알파 테스터 관문 — DONE

- (민감 · 본문 생략. 패치노트 §5 의도만 따름.)

---

## 6. 긴 빈칸이 해설 칸을 가로로 밀어냄 / 입력 UX 붕괴 — DONE

- **문제**
  - 문장형 긴 빈칸 → 제작·학습에서 해설 폭이 폭발. 줄바꿈만 넣으면 ○○○ 칸 유지·I빔·캐럿 UX가 깨짐.
- **원인**
  - 칩·wrap `white-space: nowrap`, 학습 `input` 고정폭. `textarea`/상자형 wrap은 「빈칸 안」 줄바꿈. 순수 `contenteditable`은 입력 시작 시 마스크·너비 소실.
- **구체적 수정**
  - 학습: 인라인 `contenteditable.blank-field` + `.blank-rest`(남은 ○○○). 입력 길이만큼 마스크 축소 → 칸 길이 유지 + 본문과 soft wrap.
  - 제작: `.cz-chip` nowrap 해제. `create-split` / `study-layout` `minmax(0,…)`, 패널 `min-width:0`.
  - 엿보기: 첫 줄 `getClientRects()[0]` 기준, 툴팁 하단이 첫 줄 바로 위.
  - 엿보기 flicker: 힌트 표시 중 `peekTargetInput` 유지, 마스크 DOM 직전 sticky, `retainBlankPeekAfterEdit`로 딜레이 없이 재표시.
  - 채점 노출: `정답: …` (`js/ui/prompt.js`).
  - 파일: `blank-input.js`, `prompt.js`, `study.js`(peek), `components.css` / `layout.css`, `main.js`(paste·IME·wrap 클릭).

---

## 7. 학습 범위 고르기가 「선택 카드」·단일 폴더에 묶임 — DONE

- **문제**
  - 「선택 카드」 scope가 불편. 폴더 여러 개를 한 세트에 넣고 싶음. 미리보기에서 바로 수정/학습·체크가 필요.
- **원인**
  - scope `selected` + 별도 모달. `folderId` 단일. 미리보기는 목록만.
- **구체적 수정**
  - scope `selected` 제거(마이그레이션 `→ all`). 미리보기 체크 + 전체선택/해제 (`store.studySetupCheckedIds`, persist 안 함).
  - `folderIds[]` + 조상/자손 정규화. 카드 0장이면 확정 비활성. confirm 시 빈 폴더 prune.
  - 체크 토글: `refreshStudySetupChecksOnly`(목록 재렌더 없음 → 스크롤 유지). 범위 변경 시 전체선택 리셋.
  - 미리보기 행: 수정/학습. `studyOne`은 진행 중 세션이면 확인.
  - 카드관리 breadcrumb **학습** → `startStudyFromManageFolder`.
  - 플레이바 **수정** → `editCurrentStudyCard`.
  - 파일: `queries.js`, `migrate.js`, `pickers.js`, `study.js`, `actions.js`, `manage.js`.

---

## 8. 다시 풀기·칸 이동 채점이 기대와 다름 — DONE

- **문제**
  - 다시 풀기 후에도 예전 입력/채점이 남거나, 칸을 옮겨도 채점이 안 되는 등.
- **원인**
  - 세션 저장 ↔ DOM `syncDraftFromDOM` 순서. 이탈 채점 경로 부재.
- **구체적 수정**
  - `hideAnswers`: status·`lastInput` 클리어 후 렌더·세션 저장(옛 DOM 값 재주입 방지).
  - `gradeBlank` 공용 + `gradeBlankOnLeave` (`focusout` + microtask, `onlyIfPending`). Enter 채점 유지.

---

## 9. 카드제작 — 새 카드/삭제/좁은 화면 저장 — DONE

- **문제**
  - 작성 중 새 카드로 날아가기 쉬움. 미저장인데 삭제처럼 보이는 조작. 좁은 화면에서 저장·내비 위치가 불편.
- **원인**
  - 새 카드에 dirty confirm 없음. 삭제 버튼 항상 활성에 가까움. topbar/create-head 역할 분리 부족.
- **구체적 수정**
  - 새 카드: 폼/드래프트에 작업 있으면 confirm.
  - 삭제: 저장된 카드(`cardId`)일 때만 활성 (`syncCreateDeleteButton`).
  - `body.section-create`: 좁은 화면 상단바 왼쪽에 내비, **저장** 추가. 넓은 화면 create-head sticky.
  - 파일: `actions.js`, `create.js`, `router.js`, `index.html`, `layout.css`.

---

## 10. 학습 중 「수정」 클릭이 안 먹힘 — DONE

- **문제**
  - 접힌 사이드바 쪽 투명 영역이 클릭을 가로채 학습 중 카드 수정 등이 안 눌림.
- **원인**
  - collapsed여도 `.sidebar-wrap`이 폭·이벤트를 유지.
- **구체적 수정**
  - `body.sidebar-collapsed .sidebar-wrap`: `width:0; overflow:hidden; pointer-events:none` (+ 모바일 메뉴 예외).
  - 파일: `css/layout.css`.

---

## 잔여 / 알려진 리스크

| 항목 | 상태 |
|------|------|
| 미분류 폴더 공유 | 폴더 내보내기에 없음. 의도. |
| 미리보기 부분 체크 후 같은 범위에 카드 추가 | `studySetupCheckedIds` 배열 고정 → 새 카드가 체크·학습에서 빠질 수 있음. |
| 빈칸 A→B 이동 채점 | `relatedTarget`이 `.blank-field`가 아니면 재렌더 후 포커스 유실 가능. |
| `pick-study-cards` / `openStudyCardModal` | UI scope 제거 후에도 액션 잔존(dead path). |
---

## 의도적으로 안 한 것

- 학습 기본 순서 랜덤: 넣었다가 **전량 되돌림**.
- 빈칸 최대 글자수 제한: 없음 (최소 2자만).
