# 개선 기록 — 2026-09-10

패치 `patch-notes-2026-09-10.md`와 한 쌍.
형식: **문제 / 원인 / 구체적 수정**.

---

## 1. 채점 하이라이트가 유사도·뭉치 정렬에 묶여 접두·추가 글자를 통째로 오답으로 칠함 — DONE

- **문제**
  - `쟁점` ⊂ `쟁점의 정리` 인데 `쟁점` 전체가 빨강.
  - `행정소송법상상`에서 앞 `행정소송법상`까지도 빨강이거나, 반대로 마지막 `상`이 뒤 정답의 `상`에 붙어 초록.
  - `소장부` / `소장부본`, `9000` / `9,000` 한 글자만 달라도 이후 문장이 한 칸씩 밀려 전부 오답처럼 보임.
  - 콤마 생략이 ∧(띄어쓰기 빠짐)로 나옴. 취소선·찡긋이 띄어쓰기와 무관한 자리에 붙음.
- **원인**
  - 통과용 `similarity`(띄어쓰기 뭉치 DP)를 글자 칠에도 씀. 뭉치 0/1 정렬은 비용이 같으면 **맨 뒤 뭉치**에 붙음.
  - 그다음 앞에서부터 한 글자 대치(`diff` + 양쪽 소모) → 길이만 달라도 이후가 전부 어긋남.
  - 콤마 건너뛰기를 `space-miss` 세그먼트로 그려 ∧가 나옴. 글자 누락도 같은 부호.
- **구체적 수정**
  - **통과** `checkAnswer`: 유사도 + 임계값. `normalizeTight(user)` 가 정답보다 길면 불통과. 100%는 공백 패턴까지 동일. 글자는 같고 띄어쓰기만 다르면 99(첨삭은 보이게).
  - **표시** `proofread` (`js/services/grading.js`): 앞에서 맞추고, 어긋나면 입력 건너뛰기(추가) 또는 정답 건너뛰기(누락)로 **지금 위치**에만 다시 붙임. 뒤쪽 정답의 같은 글자를 찾아가지 않음.
  - 천 단위 `,` `，` 생략: 조용히 건너뜀(∧ 없음). 사용자가 콤마를 더 쓰면 `extra`.
  - 글자 누락(본 등): 부호 없이 건너뜀. ∧는 **공백만** 건너뛸 때.
  - `extra` = 빨간 배경+취소선, `diff` = 빨간 배경만, `space-miss` = ∧, `space-extra` = ‿.
  - 남은 ○○○는 `user.length`가 아니라 `covered`(정답에서 맞춘 위치) 기준 (`prompt.js` `remainingHint`).
  - 파일: `grading.js`, `prompt.js`, `text.js`(`splitChunks`, `normalize`에 콤마·괄호 유지), `components.css` (`.bd-*`).

---

## 2. 채점 후 「정답:」이 문장 한가운데 / 본문을 다음 줄로 밈 — DONE

- **문제**
  - 정답을 흐름 속 block으로 두면 뒤 본문이 줄바꿈. 빈칸 끝에 붙이면 여러 줄 빈칸에서 가로 위치가 어긋남.
- **원인**
  - 폭 0 `holder`가 빈칸 **끝**(마지막 줄 중간)에 있고, `left`를 holder 기준으로 음수 당기면 `.study-explanation-scroll { overflow-x: hidden }`에 잘리거나 측정 전 `left:0`으로 남음.
- **구체적 수정**
  - holder는 세로 자리만. `.explanation-body { position: relative }`가 containing block.
  - `layoutBlankAnswers`: wrap `getClientRects()` 중 **가장 왼쪽**, holder 세로 + 11px, host 기준 `left`/`top`/`max-width`.
  - 자리 잡기 전 `.blank-answer` `visibility:hidden`.
  - resize / 문제 가리기 / ResizeObserver → `layoutBlankAnswersSoon`(rAF 두 번).
  - 파일: `study.js`, `components.css`, `main.js`.

---

## 3. Enter 채점 후 커서가 맨 뒤로 감 — DONE

- **문제**
  - 채점해도 입력하던 자리에 커서가 있어야 함.
- **원인**
  - `refreshStudyViews`가 innerHTML 재생성 후 `focusBlankField`가 `collapse(false)`(끝).
- **구체적 수정**
  - 채점 직전 `caretOffsetIn`, 복원 `placeCaretAtOffset`(bd-seg 포함 TreeWalker).
  - `gradeBlank`가 같은 칸 유지일 때만 offset 전달. 칸 이동은 끝.
  - 파일: `blank-input.js`, `actions.js`, `study.js`.

---

## 4. 학습 중 문제칸을 접을 수 없음 — DONE

- **문제**
  - 정답을 문제칸에 메모해 두고 가린 채 풀고 싶음. 카드 수정 없이.
- **원인**
  - 레이아웃이 문제|해설 고정 그리드.
- **구체적 수정**
  - `ui.studyPromptCollapsed` persist. `toggle-study-prompt`.
  - 접힘: 문제 패널 44px 세로 탭(≤1100px는 가로 막대), 해설에 폭 몰아줌.
  - 파일: `index.html`, `layout.css`, `router.js`, `migrate.js`, `main.js`, `components.css`(가리기 버튼 색).

---

## 5. 붙여넣기 `\r\n` · 줄바꿈 동의어 · 목차 모두 빈칸 — DONE

- **문제**
  - 윈도우 붙여넣기면 줄이 두 번 접히고 빈 줄에 캐럿이 안 잡힘. `1.`/`2.`가 동의어로 쪼개짐. 목차 일괄 빈칸이 숫자 항목을 건너뜀. 빈칸 해제 후 재선택 실패.
- **원인**
  - CR+LF가 그대로 DOM에 들어감. `splitAnswers`가 `\n`과 `||`를 같이 쪼갬. `isAutoBlankExcluded`가 숫자 토큰 제외. `chipMakeBlank`가 단일 텍스트 노드만 허용. `chipRemoveEl`이 `[contenteditable]`로 칩 자신을 잡음.
- **구체적 수정**
  - `normalizeNewlines`를 paste/read/setChip/migrate/create에 적용.
  - `splitAnswers`: `||`만 동의어. `migrateBlankAnswer`: 옛 `\n` 동의어 → aliases, 목차 연속 문단은 한 정답.
  - `wrapFirstOccurrence` TreeWalker(칩 내부 제외). 재선택: 칩만 안 걸치면 됨 + `el.normalize()`.
  - `chipRemoveEl`: `[contenteditable="true"]` 조상 + input 이벤트.
  - `chipMakeBlank` 실패 시 alert. 단어 경계에 숫자 포함.
  - 목차 모두 빈칸: `isAutoBlankExcluded` 제거.
  - 파일: `text.js`, `chip-editor.js`, `blank.js`, `migrate.js`, `outline.js`, `create.js`, `actions.js`.

---

## 6. 5분 자동저장이 카드를 확정해 버림 — DONE

- **문제**
  - 저장하지 않고 나가려는 선택이 성립하지 않음. silent 저장이 빈 문제/해설에 alert.
- **원인**
  - `autoSaveCreate` → `saveCardWithOptions({ silent: true })`가 카드 commit.
- **구체적 수정**
  - 자동저장은 `createDraft`만. 라벨 「임시저장」. 확정은 명시적 저장만.
  - silent 검증 실패는 alert 없이 `false`.
  - 파일: `actions.js`.

---

## 잔여 / 알려진 리스크

| 항목 | 상태 |
|------|------|
| 긴 빈칸에서 같은 짧은 구절 반복 (`2021`, `원고는`, `연 5%`) | `proofread`가 2글자 일치로 재동기화. 가까운 오탐이 있으면 취소선/찡긋이 엉뚱한 자리에 붙을 수 있음. 통과 점수와는 별개. |
| 천 단위 콤마 생략·글자 누락 | 첨삭 부호 없이 건너뜀. 아래 「정답:」으로만 보임. |
| `normalizeTightWithMap` | 첨삭을 `proofread`로 바꾼 뒤 미사용. |
| 정답 오버레이 | `getClientRects`+absolute. 폰트/접힘 직후 한 프레임 숨김. |
| 09-04 잔여 | 미리보기 체크 고정, A→B 포커스 유실, dead path. 그대로. |

---

## 의도적으로 안 한 것

- 첨삭에 유사도/전역 LCS를 다시 넣지 않음. (뒤쪽 같은 글자에 붙는 문제)
- 빠진 `본` 자리에 빨간 글자를 만들지 않음. (입력에 없는 글자)
- 커밋하지 않음. (이 노트 작성 시점 워킹 트리)
