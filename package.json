{
  "name": "cbt-blank-study-app",
  "private": true,
  "scripts": {
    "dev": "npx --yes live-server . --port=5173 --watch=.,css,js,index.html --no-browser",
    "build": "esbuild js/main.js --bundle --format=iife --outfile=js/app.bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
