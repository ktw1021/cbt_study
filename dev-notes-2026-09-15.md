# 개선 기록 — 2026-09-15

패치 `patch-notes-2026-09-15.md`와 한 쌍.
형식: **문제 / 원인 / 구체적 수정**.
기능 추가는 없고, 로컬 계정·가져오기·선택 상태가 어긋나던 버그만 고친다.

---

## 1. 계정 삭제 후 selectedIds가 반대로 정리됨 — DONE

- **문제**
  - 계정 삭제 뒤 남은 계정의 카드 체크가 지워지고, 삭제된 카드 id는 배열에 남았다.
- **원인**
  - `deleteUser`가 카드를 지운 다음 `filter((id) => !state.cards.some(...))` — 남은 카드를 빼고 없는 id를 남김.
- **구체적 수정**
  - `state.cards.some`으로 **아직 있는 카드만** 남김.
  - 파일: `js/app/actions.js` `deleteUser`.

---

## 2. cardId만으로 다른 로컬 계정 카드를 열고 저장하면 소유권이 넘어감 — DONE

- **문제**
  - 목록은 현재 유저만 보여 주지만 `#/create/:id`·저장·삭제·이동·바로 학습은 id만 봤다. 저장 시 `userId`를 현재 유저로 덮어썼다.
- **원인**
  - `getCard(id)`에 소유 검사가 없고, 쓰기 경로가 그걸 그대로 씀.
- **구체적 수정**
  - `getOwnedCard` / `getOwnedFolder` (`js/domain/queries.js`).
  - 열기·삭제·바로 학습·저장(기존 카드)·폴더 이동은 자기 것만이 통과. 남의 `#/create/:id`는 `#/create`로 replace.
  - 파일: `queries.js`, `actions.js`, `main.js`.

---

## 3. 계정 전환 후에도 Undo 스택이 남음 — DONE

- **문제**
  - A가 수정 → 로그아웃 → B가 되돌리기 → A 시점 전체 state가 복원될 수 있음.
- **원인**
  - `undoStack`이 모듈 전역이고 로그인/로그아웃에서 안 지움.
- **구체적 수정**
  - `clearUndo` (`js/services/undo.js`). `completeLogin`(다른 유저), `logoutUser`, `deleteUser`에서 호출.

---

## 4. 기존 카드 저장 후 URL/상세가 다른 cardId를 가리킴 — DONE

- **문제**
  - 저장 대상은 폼 `cardId`인데, 이후 URL·상세는 `store.activeManageId`라 학습 중 수정·직접 URL에서 어긋남.
- **원인**
  - 기존 카드 저장이 `activeManageId`를 안 맞춤. `editCard`도 안 맞춤.
- **구체적 수정**
  - 저장 성공·수정 진입·자기 카드 hash 진입 때 `activeManageId`를 그 카드로.

---

## 5. 폴더 가져오기 중복 fingerprint가 문제·동의어를 안 봄 — DONE

- **문제**
  - 제목·해설·빈칸 정답만 같으면 문제가 달라도, 동의어만 달라도 같은 카드로 건너뜀.
- **원인**
  - `cardFingerprint`에 `displayText`·aliases 없음.
- **구체적 수정**
  - 제목 + 문제 + 해설 + 정답 + aliases. aliases는 trim·정렬(순서만 다르면 동일). import/export 전달은 그대로.
  - 파일: `js/services/import-export.js`.

---

## 6. user/all 가져오기가 폴더 parent 순환을 그대로 넣음 — DONE

- **문제**
  - 손본 JSON에서 A↔B 순환이 들어오면 트리·카드 수가 무한 재귀에 빠질 수 있음.
- **원인**
  - `applyUsersImport`가 parent를 새 id로만 옮김. (폴더 공유 import는 순환을 루트로 끊음.)
- **구체적 수정**
  - 순환이면 가져오기 전에 거부·알림. 없는 부모는 기존처럼 루트. 정상 export는 순환이 없음.

---

## 잔여 / 알려진 리스크

| 항목 | 상태 |
|------|------|
| 이미 디스크에 순환이 있는 옛 state | 조회 함수에 방문 가드 없음. 이번 import만 거절. |
| 카드 저장 폼의 folderId | 셀렉트는 자기 폴더만. 이동 경로(`move`/`drop`)만 폴더 소유를 검사. |
| 같은 계정 Undo | 전환 시에만 비움. 같은 로그인 세션 Undo는 그대로. |

---

## 의도적으로 안 한 것

- 조회 함수 전부에 순환 가드를 넣지 않음.
- fingerprint를 user/all 가져오기에는 쓰지 않음 (원래 없음).
- Undo 스냅샷 구조를 계정별로 나누지 않음.
