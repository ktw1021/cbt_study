# 개선 기록 — 2026-09-12

패치 `patch-notes-2026-09-12.md`와 한 쌍.
형식: **문제 / 원인 / 구체적 수정**.

---

## 1. 카드제작 해설 칸이 작아 긴 글을 빈칸으로 다루기 힘듦 — DONE

- **문제**
  - 해설 편집기(`.xl`) 기본 높이가 약 420px. 문제|해설 좌우 분할이라 가로도 절반이다.
  - 긴 해설에서 문장을 찾고 드래그해 빈칸을 만들려면 칸 안 스크롤을 반복해야 한다.
- **원인**
  - 제작 레이아웃이 미리보기·목차·빈칸 목록과 편집기를 한 패널에 쌓는다. 편집기만 키울 경로가 없었다.
- **구체적 수정**
  - **집중 편집**은 UI 표시만 바꾼다. `#explanationTemplate`을 복제하지 않는다. 저장·빈칸은 기존 `save-card` / 칩 에디터를 그대로 쓴다.
  - store에 필드를 넣지 않는다. `body.create-explanation-focus`만 켠다 (`js/ui/create.js`).
  - 해설 칸 라벨 오른쪽 **집중 편집**(연한 보라). 켜면 같은 해설 패널을 `position: fixed; inset: 0`으로 키운다.
  - 집중 중에는 목차·미리보기·빈칸 목록·정답·메모(`.create-explanation-aside`)를 숨긴다. 헤드에 닫기·저장.
  - Esc로 종료. `#modalRoot`에 모달이 있으면(빈칸 후보 등) 집중 편집은 닫지 않는다.
  - 카드제작을 떠나면 `showSection`에서 끈다. 켤 때 `scrollY`를 기억해 끄면 되돌린다.
  - 평소 레이아웃 CSS는 그대로. 집중 클래스일 때만 overlay·에디터 `flex: 1`.
  - 파일: `index.html`, `css/components.css`, `js/ui/create.js`, `js/main.js`, `js/ui/router.js`.

---

## 2. 같은 줄·가까운 오답의 「정답:」이 겹침 — DONE

- **문제**
  - 채점 후 각 오답 아래에 `정답:` 을 띄우는데, 한 줄에 빈칸이 여러 개이거나 가까우면 글이 포개진다.
- **원인**
  - `layoutBlankAnswers`가 빈칸마다 같은 방식(줄상자 왼쪽, 홀더 아래 +11px)으로만 앉힌다. 가로 겹침을 보지 않는다.
- **구체적 수정**
  - 해설 본문(`.explanation-body`) 단위로, 이미 놓은 `정답:` 박스(`left/top/width/height`)와 AABB로 충돌만 본다.
  - 안 겹치면 기존 위치(자기 빈칸 아래 첫 행)를 유지. 겹치면 줄 높이만큼 내려 다시 검사하고, 비는 가장 가까운 행에 둔다. 홀수/짝수 교대 없음.
  - 행을 내린 만큼 자리표시자 `height` / `vertical-align`을 늘려 아랫줄 본문과 겹치지 않게 한다.
  - 입력·채점·표시 HTML은 그대로. `js/ui/study.js` `layoutBlankAnswers`만.

---

## 잔여 / 알려진 리스크

| 항목 | 상태 |
|------|------|
| 좁은 뷰포트에서 집중 편집 | 툴바·헤드를 빼면 에디터 높이가 평소 420px보다 작아질 수 있다. 넓은 화면이 본 용도. |
| 집중 중 상단 내비 | overlay(z-index 400)가 가린다. 닫기/Esc 또는 기존 저장으로 나간다. |
| 빈칸 후보 모달 Esc | 모달은 원래 Esc를 안 받는다. 집중 편집만 가드한다. |
| 한 줄에 긴 정답이 여러 개 | 앞 정답이 여러 줄이면 뒤 정답은 행을 더 내려 피한다. 본문 순서상 앞칸이 첫 행을 가진다. |

---

## 의도적으로 안 한 것

- 새 해설 에디터·새 저장/빈칸 함수를 만들지 않음.
- 집중 편집 여부를 persist하지 않음.
- 카드제작 스플릿 레이아웃을 다시 짜지 않음.
- 정답 표시를 홀수/짝수 줄로 나누지 않음. 실제 박스가 겹칠 때만 행을 늘림.
