# 개선 기록 — 2026-08-15

학습 흐름(회독·정답 확인·다음 문제) 개편 + 그 과정에서 드러난 집계 버그.
형식: 문제 / 원인 / 구체적 수정.

---

## 1. 정답 처리 후 정답을 볼 수 없음

- **문제**
  - 빈칸 하나 채우고 Enter로 정답 처리하면, 그 빈칸에 다시 마우스를 대도 정답이 안 보임.
  - 유사도 통과 = 실제 정답이 아니므로, 정답 처리 후에도 실제 정답을 볼 수 있어야 함.
- **원인**
  - `js/ui/study.js` `canBlankPeek()` — `checked`면 **입력값이 비었을 때만** 호버 허용.
    Enter 채점 후엔 입력값이 남으므로 **채점된 모든 빈칸에서 호버가 영구 차단**.
  - 인라인 정답 라벨(`js/ui/prompt.js`)은 `reveal = checked && (!correct || score < 100)`.
    **100% 일치는 라벨도 없고 호버도 막혀** 확인 경로가 아예 없었음.
  - 라벨·툴팁 모두 `splitAnswers(...)[0]` 하나만 노출 → 동의어 확인 불가.
- **구체적 수정** (DONE)
  - `canBlankPeek()` **제거**. 채점 전후 모두 호버 허용. 대기 게이지가 문턱 역할을 함.
  - `peekAnswerText(order)` 신설 — `splitAnswers(...).join(' / ')`로 인정 답안 전부 표시.
  - `peekDelayFor(order)` — 채점 전 1.5초 유지, **채점 후 0.3초**(`PEEK_DELAY_GRADED_MS`).
    `PEEK_DELAY_MS` 상수 직접 참조를 모듈 변수 `peekDelayMs`로 교체.
- **되돌린 것**
  - 1차 수정에서 "채점 전 + 입력값 있으면 차단"(커닝 방지)을 넣었으나 **원래 없던 제약**이었고
    의도에도 어긋나 철회. 볼지 말지는 사용자가 정한다.

---

## 2. 회독을 수동으로 올려야 함

- **문제**
  - 전체 빈칸을 다 맞혀도 회독은 그대로. 「회독 +1」을 손으로 눌러야 함.
- **원인 — 의도된 제약이었음**
  - README에 "회독은 자동으로 올라가지 않는다. 메모용 카운터" 명시.
  - 채점 진입로가 여럿(칸별 Enter / 전체 채점 / 다시 풀기 / 채점 후 수정 재채점)이라
    "언제가 1회독인가"를 정의하기 어려웠고, 회독은 `low-rounds` 정렬 기준이라 오염 시 영향이 큼.
- **진짜 원인 — 멱등성 부재 (선행 버그)**
  - `applyCardResult()`가 **호출될 때마다** `wrongCount`를 +1.
    `gradeBlankOnEnter`는 "모든 빈칸 checked"면 호출하는데, 이미 다 채점된 상태에서
    한 칸 고쳐 다시 Enter → 조건 재충족 → **오답 횟수가 시도마다 누적**.
  - `store.studyAttemptRecorded`가 정확히 이 용도로 존재(세팅·리셋·세션 저장까지 구현)했으나
    **아무도 읽지 않음**. 회독을 그대로 얹었으면 회독도 같이 부풀었을 것.
- **구체적 수정** (DONE)
  - `applyCardResult()` 재정의 — 필드 성격별로 분리:
    - `lastResult` = '지금 상태' → **매번 갱신**
    - `wrongCount` = 누적 → `studyAttemptRecorded` 가드, 시도당 1회
    - `rounds` = 누적 → `studyRoundRecorded` 가드, 시도당 1회
    - 반환값: 이번 호출로 회독이 올랐는지 (`boolean`)
  - `js/core/store.js`에 `studyRoundRecorded` 추가 (+ 미선언이던 `_studyCardId`도 명시).
  - `js/services/study-session.js` `cardProgress`에 `roundRecorded` 저장·복원.
    **추가 전용 필드** — 예전 세션은 `undefined` → `false`로 시작. 마이그레이션 불필요.
  - 리셋 지점: `hideAnswers()`(다시 풀기), `renderStudyCard()` 신규 상태 생성, `clearStudySession()`.
- **채택한 회독 규칙 (lenient)**
  - 틀렸다가 고쳐서 최종적으로 전부 정답이면 **+1 인정**.
  - 단 한 시도에서 회독은 최대 1회. 「다시 풀기」로만 새 시도 시작.
  - `rounds`·`wrongCount`·`lastResult` 모두 **기존 필드**. 스키마 변경 없음.

---

## 3. 회독 자동 증가를 사용자가 알 수 없음

- **구체적 수정** (DONE)
  - `js/ui/study.js` `showRoundToast(order, text)` — 마지막 채점 빈칸 옆에 `+1 회독 성공`.
    `document.body`에 절대배치 + `getBoundingClientRect()` — **호버 툴팁(`ensurePeekEl`)과 동일 패턴 재사용**.
  - `animationend`에 resolve하는 Promise 반환 → 호출부에서 다음 단계를 이어 붙임.
    `setTimeout(1600)` 안전망(애니메이션 미발화 시 누수 방지).
  - CSS `.round-toast` + `@keyframes round-toast-rise` (1.2s, 위로 증발), `prefers-reduced-motion` 대응.

---

## 4. 다음 문제 / 세트 완료 흐름 없음

- **문제**
  - 다 맞혀도 「다음 →」을 손으로 눌러야 함. 마지막 문제를 끝내면 **아무 일도 안 일어남**.
- **원인**
  - `nextCard()`가 `if (index < length - 1)`만 있고 `else` 없음 → 마지막에서 조용히 무시.
    "세트 완료"라는 상태 개념 자체가 없었음.
  - 앱 전체가 native `alert`/`confirm`만 사용. 3지선다 불가 + 동기 블로킹이라 토스트 애니메이션과 못 이어짐.
- **구체적 수정** (DONE)
  - `js/ui/choice-modal.js` **신설** — `openChoice({title, message, choices}) → Promise<value|null>`.
    첫 버튼 `focus()`로 **Enter = 확인**, Esc(capture 단계)·배경 클릭 = 취소.
    기존 `.modal-backdrop`/`.modal` CSS 재사용, 전용 루트 `#choiceRoot`(다른 모달의 `closeModal()`과 분리).
  - `celebrateRound()` → `showRoundToast()` → `promptAfterRound()` 체인.
  - `promptAfterRound()`: 토스트 1초 사이 답을 고쳤을 수 있으므로 **카드 동일성 + 전부 정답 재확인** 후 진행.
  - `finishStudySet({completed})`: 「홈으로(=카드관리) / 학습모드로 / 취소」.
    **선택해서 화면을 떠날 때만** `clearStudySession()` — 취소하면 그대로 남아 곱씹기 가능.
  - `nextCard()`가 마지막 카드에서 `finishStudySet({completed:false})` 호출 → 수동·자동 경로 결과 일치.
  - `showSection()`에 `closeChoice()` — 화면 전환 시 모달 잔류 방지.
- **Enter 충돌**
  - `main.js`의 Enter 채점은 `document.activeElement`가 빈칸 input일 때만 동작.
    모달이 버튼으로 포커스를 가져가므로 겹치지 않음. 실측 확인함.

---

## 5. 카드관리 상세 패널이 학습 결과를 반영 안 함

- **문제**
  - 카드관리에서 「바로 학습」 → 회독하고 돌아오면 **상세 패널의 회독·오답 숫자가 그대로**.
    카드 목록(왼쪽)은 갱신되는데 상세(오른쪽)만 옛날 값.
- **원인**
  - `js/ui/render.js` `renderAll()`이 `renderSidebar`·`renderFolderTree`·`renderFolderSelects`·`renderCardList`만 호출.
    **`renderManageDetail`은 빠져 있었음.** 상세는 액션이 명시적으로 부를 때만 갱신됨.
  - 선택 상태(`store.activeManageId`)는 유지되는데 그 선택으로 **데이터를 다시 읽지 않은** 것이 문제.
    선택 기억 ≠ 데이터 동결.
- **구체적 수정** (DONE)
  - `renderAll()`에서 `store.activeManageId`가 있으면 `renderManageDetail(activeManageId)`,
    없으면 `renderCardList()`. (`renderManageDetail`이 내부에서 `renderCardList`를 부르므로 이중 렌더 회피)
  - `render.js`의 `export { renderManageDetail } from './manage.js'` 재수출을 import + re-export로 변경.

---

## 6. 코드 정리 (같이 처리)

- `makeCreateSnapshot` — `app/actions.js`와 `ui/router.js`에 **글자 단위 동일 중복**.
  한쪽만 고치면 "저장 안 된 변경" 경고가 조용히 어긋남 → `ui/create.js` 단일 정의로 통합.
- `syncDraftFromDOM`(study.js) / `syncBlankDraftFromDOM`(study-session.js) 동일 로직 중복
  → `js/ui/blank-input.js` 신설로 통합. `getBlankInputValue`도 함께 이동.
  **순환 참조 회피용 별도 모듈** (study.js ↔ study-session.js). study.js가 re-export해 기존 import 유지.
- `deleteCurrentCard` → `deleteCardById` 경로에서 **confirm 두 번** → 한 번으로.
- Ctrl+1~7 플래그 단축키가 입력 중에도 발동.
  `applyFlag`는 `studyQueue[studyIndex] || 편집 카드` 대상이라 **화면에 없는 카드에 플래그가 찍힐 수 있었음**
  → `input/textarea/[contenteditable]` 포커스 시 제외.
- 죽은 export 제거: `study.js`의 `paintStatuses`·`refreshStudyPrompt`·`renderReveal`(빈 함수),
  `resume-panel.js`의 `renderAllResumePanels`(+ 미사용 `MOUNTS`).
- `migrate.js` `merged.ui`의 `studySession: null` 키 중복 선언 제거.
- `js/app.bundle.js` 삭제(6/15 빌드본, 소스는 6/25 — 아무도 로드 안 함).
- README: 포트 5173 → 3000(package.json은 6/17에 이미 3000, **문서만 뒤처져 있었음**), 회독·정답 확인·완료 흐름 갱신.
- `ui/patch-notes.js` `PATCH_FILES` 맨 위에 `patch-notes-2026-08-15.md` 등록.
  이 목록은 수동 관리이고, **파일이 없으면 `catch`로 조용히 건너뛴다**(에러 없이 항목만 사라짐).
  md 파일을 커밋에서 빠뜨리면 배포본에서 새 패치노트가 안 보이므로 항상 같이 올릴 것.

---

## 7. 수동 정답/오답 처리 함수 삭제

- **무엇이었나**
  - `markSingleBlank(order, isCorrect)` — 빈칸 하나를 사람이 정답/오답으로 확정(점수 100/0 덮어쓰기, `blank.manualResult` 기록).
  - `markCardAll(isCorrect)` — 카드 전체를 「전체 정답 처리」/「전체 오답 처리」.
  - 용도는 유사도 자동 채점의 오심 교정("고의 또는 과실" vs "고의·과실"이 78%로 오답 처리되는 등).
- **한 번도 실행된 적 없음**
  - `git log -S "markCardAll"` → 초기 커밋 1건뿐. `mark-*` data-action 문자열은 **히스토리 전체에 0건**.
  - 버튼·단축키·호출부 전무 → `blank.manualResult`는 모든 카드에서 항상 `null`.
- **삭제 결정 근거**
  - 회독 자동화가 붙으면서 가치가 생기긴 했으나(억울한 한 칸 때문에 회독이 막힘),
    「전체 정답 처리」는 **회독을 공짜로 올리는 경로**가 됨. 회독은 `low-rounds` 정렬 기준이라 학습 순서가 왜곡됨.
  - 쓸지 말지 정하지 않은 채 남겨두면 계속 오해를 부름. 필요해지면 git에서 꺼낸다.
- **남긴 것**
  - `blank.manualResult` 필드는 `migrate.js`·`blank.js`에 **그대로 둠**.
    항상 `null`이지만 저장된 데이터의 모양을 이유 없이 바꾸지 않기 위함.

---

## 8. 동의어 입력 복원 — `aliases` 필드 연결

- **문제**
  - 채점 엔진은 `||`·줄바꿈 동의어를 계속 지원하는데 **만들 UI가 없었음**. 읽을 줄만 알고 쓸 수 없는 상태.
- **원인 — 리팩터의 부수 피해**
  - 초기 커밋엔 빈칸별 `<textarea placeholder="동의어: 줄바꿈 또는 ||">`가 있었음.
  - 6/15 칩 에디터 리팩터(a29013f)에서 정답 출처가 "해설에서 드래그한 단어"로 일원화 →
    폼에서 정답을 편집할 이유가 없어져 `answer-readonly` div로 대체. 이때 동의어 입력이 함께 사라짐.
  - `blanks[].aliases`는 스키마·스냅샷·마이그레이션에 계속 실려 다녔지만 **채점에서 읽지 않았음**.
- **구체적 수정** (DONE)
  - `domain/blank.js` `acceptedAnswers(blank)` 신설 — `splitAnswers(answer) + aliases` 합쳐 중복 제거.
    예전 카드는 `answer` 자체에 `||`가 들어 있으므로 **양쪽 모두 지원**.
  - 채점 두 곳(`gradeBlankOnEnter`, `gradeCurrent`)의 `splitAnswers(b.answer)` → `acceptedAnswers(b)`.
    호버 툴팁(`peekAnswerText`)도 동일.
  - 칩이 동의어를 실어 나르게 함 — `chip.dataset.aliases`(`||` 구분).
    `makeChipEl(answer, aliases)` / `setChipEditorContent` / `nodeToTemplate`·`readChipEditor` 확장.
    **이게 핵심**: 안 그러면 해설을 한 글자 칠 때마다 `readChipEditor`가 aliases 없는 blanks를 만들어 입력이 날아감.
  - `chipSetAliases(id, order, text)` 추가. `main.js` `onInput`이 `input[data-alias-for]`를 받아
    **칩만 갱신하고 슬롯은 재렌더하지 않음**(타이핑 중 커서 유실 방지).
  - 카드제작·학습 중 수정 폼 양쪽 슬롯에 `aliasInputHtml()` 추가.
- **주 정답은 여전히 읽기 전용**
  - 본문 단어 = 주 정답이라는 칩 에디터의 단일 출처 원칙을 깨지 않음. 동의어만 덧붙인다.
  - `blankShapeHint`(○○○ 힌트)는 주 정답 기준 유지. 동의어는 길이가 달라도 됨.
- **스키마**
  - `aliases`는 **이미 있던 필드**. 값이 없으면 `[]`. 마이그레이션 불필요.

---

## 9. 저장소·개발환경 정리

- **지운 것**
  - `build` 스크립트(esbuild)와 `devDependencies` — `index.html`이 `js/main.js`를 모듈로 직접 읽으므로
    번들이 필요 없음. 산출물 `js/app.bundle.js`도 이미 삭제됨(6/25 노트에도 미사용으로 기록).
  - **`node_modules/` 추적 파일 14개** — `.gitignore`에 `node_modules/`가 있었지만
    **ignore는 이미 추적 중인 파일에 적용되지 않아** 계속 커밋되고 있었음.
    `@esbuild/win32-x64/esbuild.exe` **10.6MB** 포함. 정적 사이트 저장소에 윈도우 바이너리가 실려 있던 셈.
  - `package-lock.json` — 남는 의존성이 없어 잠글 대상이 사라짐.
  - `.gitignore`의 `js/app.bundle.js` — 생성하는 스크립트가 없어졌으므로 불필요.
  - `dev` 스크립트의 `--watch=.,css,js,index.html` — 맨 앞 `.`이 폴더 전체를 이미 감시하므로 나머지는 무의미.
    (로그에 `Change detected README.md`가 찍힌 것이 증거)
- **바꾼 것**
  - `--host=localhost` 추가 → 터미널 안내가 `http://localhost:3000`으로 나옴.
  - `--no-browser` 제거 → 서버 켜면 브라우저 자동으로 열림.
- **`--host=localhost`의 부작용 (윈도우)**
  - Node가 `localhost`를 `::1`로 풀어 **IPv6 루프백에만 바인딩**함(`Serving "." at http://localhost:3000 (http://::1:3000)`).
    즉 `127.0.0.1:3000`은 더 이상 응답하지 않음. 주소가 하나로 강제되는 효과.
  - 예전 `127.0.0.1:3000` 오리진에 쌓인 데이터를 꺼내야 하면 임시로:
    `npx --yes live-server . --host=127.0.0.1 --port=3000` → 내보내기(JSON) → 다시 `npm run dev`로 가져오기.
- **GitHub Pages 영향 없음**
  - `.github/` 워크플로 자체가 없음 → 브랜치의 정적 파일을 그대로 서빙.
  - `package.json`은 배포에서 읽지 않음. 지운 것 중 `index.html`이 참조하는 파일은 하나도 없음.
- **오리진 = scheme + host + port** (6/17 노트 재확인)
  - `localhost:3000` / `127.0.0.1:3000` / GitHub Pages 주소는 전부 **별개 IndexedDB**.
    6/17에 5173→3000으로 포트를 바꾼 것도 같은 이유였음. README에 주의 문구 추가함.

---

## 점검 — 알려진 잔여

| 항목 | 상태 |
|------|------|
| `chipSetAnswer` (chip-editor.js) | 정의만 있고 호출 없음. 칩의 표시 텍스트까지 바꾸므로 동의어 용도로는 부적합(본문 단어가 바뀜). 정답 슬롯을 다시 편집 가능하게 만들 때만 쓸 물건. |
| `blank.manualResult` | 쓰는 코드 없음(항상 `null`). 데이터 모양 유지 목적으로만 존치. |
| PIN 평문 저장 | 로컬 전용이라 위협 모델상 수용 가능하나 **내보내기 JSON에 그대로 실려 나감**. |
| `.gitignore`의 `AGENT.md` | 원칙 문서가 공유되지 않음. 의도인지 확인 필요. |
| IndexedDB·스키마 | `rounds`·`wrongCount`·`lastResult`·`aliases` 모두 기존 필드. `cardProgress.roundRecorded`만 학습 세션 한정 추가 필드. 저장 키·필드명 변경 없음. |
