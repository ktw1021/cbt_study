# 개선 기록 — 2026-06-17

테스터 피드백(건의 3~6) 기반. 형식: 문제 / 원인 / 해결방향 / 구체적 수정.
(2026-06-15 기록과 별개. 그날 파일은 그대로 둠)

---

## 1. 폴더 「루트 폴더」 버튼 정리 (건의3)

- **문제**
  - 「루트 폴더」 버튼 라벨이 길고, 색(연보라 배경/남색 글씨)이 잘 안 보임. 펼치기/접기와 묶여 있어 위치도 애매. 루트/하위 폴더 추가 버튼 스타일이 제각각.
- **해결방향**
  - 라벨을 `+`로 통일, 「폴더」 제목 바로 오른쪽(왼쪽 그룹)으로 분리. 루트·하위 폴더 추가 버튼을 **같은 클래스로 모듈화**하고 연두 파스텔로(삭제 ×의 빨강과 대비).
- **구체적 수정** (DONE)
  - `index.html`: 폴더 헤더를 `[제목 +] ……… [펼치기 접기]` 구조로 변경(`.folder-head-left` 래퍼). 루트 버튼 클래스 `folder-add`.
  - `js/ui/manage.js`: 하위 폴더 `+` 버튼도 동일 클래스 `folder-add` 사용(공통화).
  - `css/components.css`: `button.folder-add`(연두 파스텔 `#ecfccb`/글씨 `#3f6212`/테두리 `#d9f99d`, hover `#d9f99d`), `.folder-head-left`(flex).

---

## 2. 학습모드 진입 + 선택 학습 (건의4)

- **문제 제기**
  - "학습모드에서 바로 학습 시작하는 법을 모르겠다. 왼쪽 필터가 작동 안 하는 것 같다."
- **점검 결과**
  - 필터는 정상 동작. 검색은 제목·원문·해설·메모·정답까지 매칭(`getCardsFiltered`), 플래그/최근결과도 적용. `onChange`/`onInput` → `renderAll`.
  - 다만 필터 결과가 반영되는 곳은 카드관리 목록과 사이드바 「선택 카드」뿐이고, 학습 시작은 `studyMode` 드롭다운만 사용 → 학습과 연결돼 있지 않아 "죽은 것처럼" 보였음.
- **해결방향 (합의)**
  - 공존·역할 분리: 사이드바 「선택 카드」 = 수동 골라담기 학습 진입점, 학습모드 setup의 드롭다운 = 조건 자동 학습. 선택 학습은 순서/랜덤 토글 제공, 시작 후 선택집합 유지.
- **구체적 수정** (DONE)
  - `index.html`: 「선택 카드」 패널에 순서/랜덤 셀렉트(`#selectedStudyOrder`) + 「선택 카드 학습 시작」 버튼(`#startSelectedBtn`) + 선택 개수 표기(`#selectionCount`).
  - `js/app/actions.js`: `startSelectedStudy()` — `buildStudyQueue('selected', …, selectedIds)` + 랜덤 옵션 셔플 → `study-play`. 기존 `startStudy` 로직 재사용.
  - `js/ui/sidebar.js`: 선택 개수 표시, 0개면 시작 버튼 `disabled`.
  - `js/main.js`: `start-selected-study` 액션 매핑.
  - `css/components.css`: `.sel-inline`, `button:disabled`.
  - 데이터/스토어 스키마 변화 없음(`selectedIds` 그대로) → 배포 데이터 안전.
- **보정 (DONE)**
  - 필터 불일치 버그: 검색으로 1개만 보여도 「N개 선택」이 전체 선택수(예: 2)로 뜨고 학습도 전부 묶여 진행됨. 원인은 개수/학습이 `selectedIds`(필터 무관 전체)를 봤기 때문.
  - 수정: **현재 필터에 보이는 카드 ∩ 선택**으로 일치시킴. `sidebar.js`(`statSelected`/`selectionCount`/시작 버튼 활성), `actions.startSelectedStudy`(`getCardsFiltered(readFilters(), …)` ∩ `selectedIds`). selectedIds 자체는 안 바꿈(단순·유지보수 용이).
  - 버튼 라벨 「선택 카드 학습 시작」 → 「학습 시작」.
- **핫픽스 (DONE)**
  - 위 보정 중 `actions.js`에 `import { readFilters }`를 **중복 선언**(기존 import 이미 존재) → `SyntaxError: Identifier 'readFilters' has already been declared`로 `main.js` 모듈 그래프 전체가 로드 실패. 결과: 이벤트 리스너 미연결 → 인증 오버레이가 정적 HTML 기본값(회원가입 탭)에 고정되고 로그인 탭 전환·가입 모두 먹통.
  - 수정: 중복 import 제거(line 27). 브라우저에서 모듈 로드 OK + 로그인/회원가입 탭 전환 정상 확인.
  - 교훈: import 추가 시 동일 심볼 기존 import 여부 확인. 단일 SyntaxError가 앱 전체를 죽임(정적 오버레이가 회원가입에 멈추는 형태로 드러남).

---

## 3. 좌상단 Anki 문구 제거 + 패치노트 페이지 (건의5)

- **문제**
  - 사이드바 상단의 "Anki식 폴더 관리 …" 안내 문구가 불필요. 대신 패치노트를 공지사항처럼 **별도 페이지**에서 하나하나 보고 싶음(모달 X).
- **해결방향**
  - 문구 제거 → 「패치노트」 버튼이 **라우팅된 페이지(`#/patch`)** 로 이동. `patch-notes-*.md`를 불러와 최신순 아코디언(`<details>`)으로 렌더.
- **구체적 수정** (DONE)
  - `index.html`: `.sub` 문구 → `#navPatch`(`data-action="nav-patch"`) 버튼. `#patchSection` 섹션 + `#patchNotesBody` 추가.
  - `js/core/hash-router.js`: `parseRoute`/`routeToPath`에 `patch` 라우트 추가(`#/patch`).
  - `js/ui/router.js`: `showSection`에서 `#patchSection` 토글, `syncNavActive`에 `navPatch` 포함/리셋.
  - `js/main.js`: `handleHashRoute` patch 분기, `nav-patch` 액션, `renderPatchPage` import.
  - `js/ui/patch-notes.js`: `renderPatchPage()` — `PATCH_FILES`(최신순) fetch → 첫 `# 제목`을 summary로, 본문은 초소형 마크다운 렌더 → `<details>` 아코디언(맨 위 패치 open). 실패 파일은 건너뜀.
  - `css/components.css`: `.patch-body`/`.patch-entry`/`.patch-summary`/`.patch-content` 스타일.
  - **정적 호환**: fetch 기반이라 GitHub Pages 등 정적 호스팅에서 그대로 동작(서버 불필요). 단 `file://` 직접 열기 시 fetch 차단될 수 있음.
  - 새 패치 추가 시 `PATCH_FILES` 맨 위에 파일명만 추가.

---

## 4. 입력창 커서 자동 스크롤 (건의6)

- **문제**
  - 카드제작 해설 입력칸이 꽉 찬 상태에서 Enter를 치면, 한글 워드처럼 커서를 따라 화면이 내려가지 않고 사용자가 수동 스크롤해야 함. 전체 입력창 공통 불편.
- **원인**
  - 해설 칸은 contenteditable 칩 에디터인데 Enter를 `preventDefault` 후 직접 `\n` 삽입(`chip-editor.js`). 프로그래matic 편집이라 브라우저의 "커서로 자동 스크롤"이 동작하지 않음.
  - textarea는 네이티브로 스크롤되긴 하나 커서가 맨 아래 경계에 붙어 여백이 없음.
- **해결방향**
  - 입력 시 커서 위치를 측정해 약간의 여백(28px)을 두고 스크롤 컨테이너를 조정. textarea·contenteditable 공통 처리. 모듈화.
- **구체적 수정** (DONE)
  - `js/ui/caret-scroll.js` 신설:
    - `scrollContentEditableCaret`: Selection range rect → 컨테이너 내부 좌표 변환 후 스크롤. 빈 줄로 rect를 못 얻으면 임시 마커(zero-width)로 측정 후 커서 복원.
    - `scrollTextareaCaret`: 오프스크린 미러 div로 커서 y좌표 측정 후 스크롤(스타일 복제, box-sizing border-box).
    - `initCaretAutoscroll`: 문서 전역 `input` 위임. target이 textarea면 textarea 처리, `chip-editor`면 contenteditable 처리. `requestAnimationFrame`으로 레이아웃 후 측정.
  - `js/main.js`: `initCaretAutoscroll()`를 `bindEvents`에서 1회 초기화.
  - chip-editor 등 기존 모듈은 건드리지 않음(위임 리스너만 추가) → 부수효과 최소.

---

## 5. 페이지 이동 시 화면이 갱신되지 않음 (카드 제작 후 새로고침해야 보임)

- **문제**
  - 카드 제작 후 「카드관리」로 가면 새 카드가 안 보이고, 새로고침해야 보임. 전반적으로 "이전 화면이 그대로 남는" 느낌.
- **원인**
  - 상단/사이드바 내비는 `showSection` → `syncUrl` → `history.pushState`만 호출. pushState는 `hashchange`/`popstate`를 발생시키지 않아 `handleHashRoute`의 `renderAll`이 실행되지 않음 → 직접 내비 시 리렌더 누락.
  - (보조) `store.activeFolderId`는 런타임 값이라 새로고침 시 초기화 → 폴더 필터가 풀리며 카드가 다시 보이는 착시.
- **해결방향**
  - 직접 내비게이션도 대상 화면을 새로 렌더.
- **구체적 수정** (DONE)
  - `js/main.js`: `nav-manage`/`nav-study`에서 `showSection` 후 `renderAll()` 호출. `nav-patch`는 `renderPatchPage()`. (`save-card`는 이미 `renderAll` 호출 중)

---

## 6. 로컬 개발 서버 — 포트 고정 + 라이브 리로드

- **문제**
  - `npm run dev`(live-server)가 5173 포트라 `serve`(3000)에서 쌓은 테스트 데이터가 안 보임. IndexedDB는 origin(scheme+host+port)별이라 포트가 다르면 데이터가 분리됨.
- **해결방향**
  - dev 서버 포트를 3000으로 고정해 기존 데이터 origin을 재사용. 라이브 리로드 유지.
- **구체적 수정** (DONE)
  - `package.json`: `dev` 스크립트 포트 `5173 → 3000`.
  - 사용: 기존 `serve`를 Ctrl+C로 종료 후 `npm run dev` → `http://localhost:3000` 접속/새로고침 1회 → 이후 파일 수정 시 자동 리로드.
  - 배포는 정적 그대로(GitHub Pages). dev 서버는 로컬 전용.

---

## 7. 채점 후 정답 노출 + 수정 가능 + 4:6 레이아웃 (추가3)

- **문제**
  - (1) 유사도 임계값으로 "맞음" 처리되면 정답이 굳어버려(`readonly`) **수정·글자 삭제 불가**. 적당히 비슷하지만 틀린 답도 못 고침 → 치명적.
  - (2) 100% 일치가 아닌 정답인데 정답을 안 보여줘서 **못 쓴 부분을 학습 못 함**(예: "…제46023호"까지만 써도 통과되면 뒤 문장을 못 봄).
  - (3) 오답 시 정답을 `→정답`으로 입력칸 **우측 인라인 `nowrap`** 표시 → 긴 정답에서 가로 스크롤 필요.
- **원인**
  - `js/ui/prompt.js blankInputHtml`: `revealed && correct` 분기에서 `readonly` + 값을 정답으로 덮어씀. `revealed && !correct` 분기는 `.blank-hint`를 입력칸 옆 인라인으로 출력. `.blank-wrap`이 `white-space:nowrap`, 해설 스크롤은 `overflow-x:auto`라 긴 정답이 가로로 넘침.
  - `js/ui/study.js paintInlineBlanks`: `s.revealed && s.correct`면 `input.readOnly = true`로 잠금. `syncDraftFromDOM`은 채점된 칸(`s.checked`)을 건너뛰어 채점 후 편집값이 모델에 반영 안 됨.
- **해결방향**
  - 입력칸은 채점 여부와 무관하게 **항상 편집 가능**. 채점 후 `checked && (오답 || score<100)`이면 정답을 **입력칸 바로 아래**에 작게·줄바꿈으로 노출(가로 스크롤 제거, 다음 글자와 안 붙음). 100% 일치(띄어쓰기 차이 포함)면 노출 안 함. 문제:해설 = **4:6**.
- **구체적 수정** (DONE)
  - `js/ui/prompt.js`: `readonly` 분기 제거, 입력칸 단일화(항상 편집 가능, 값은 사용자 입력 그대로). `reveal = checked && (!correct || score<100)`이면 `<span class="blank-answer (partial|wrong)">정답 …</span>`을 래퍼 안에 추가. 래퍼에 `revealing` 클래스.
  - `js/ui/study.js`: `paintInlineBlanks`에서 `input.readOnly = false`로 고정(잠금 해제). `syncDraftFromDOM`이 채점 여부 무관하게 현재 DOM 값을 흡수 → 채점 후 수정·삭제가 보존되고, 다시 Enter 시 새 값으로 재채점(`gradeBlankOnEnter`는 DOM 값을 직접 읽음).
  - `css/components.css`: `.blank-wrap.revealing`(inline-flex 세로배치), `.blank-answer`(아래·작게·줄바꿈·`max-width:34rem`, partial=초록/wrong=빨강). 미사용이 된 `.blank-hint`/`.bad-hint` 제거. `.create-split` `1fr 1fr → 4fr 6fr`.
  - `css/layout.css`: `.study-layout` `1fr 1fr → 4fr 6fr`. `.study-explanation-scroll { overflow-x: hidden }`(정답 줄바꿈으로 처리, 가로 스크롤 차단).
  - 참고: `js/app.bundle.js`는 `index.html`(모듈 `main.js` 직접 로드)에서 미사용이라 재빌드 불필요.

---

## 8. 화면 이동 일관화 + 학습 흐름 재설계 + 루트 복귀 (추가4)

- **문제 (사용자 지적)**
  - "새로고침 패치가 국지적인 듯. 선택은 남고, 학습은 시작 안 해도 풀리고, 카드제작은 이동하면 지워지고 — 일관성이 없다." / "학습방식이 불명확(순서가 무슨 순서? 선택 폴더가 어디?)." / "카드관리에서 루트로 돌아갈 방법이 없다."
- **원인**
  - 기존 "새로고침 패치"는 메뉴 클릭 핸들러에 `renderAll()`만 추가한 목록 리페인트일 뿐, 페이지 이동 개념이 아니었음. 상태 수명이 3종(선택=영구 / 학습세션=복원 / 제작초안=휘발)으로 비대칭.
  - `buildStudyQueue(mode,…)`의 mode 문자열은 순서=저장순(미표기), folder는 `activeFolderId`(카드관리에서만 설정, 학습화면에 표시 없음, 미선택 시 전체로 샘)였음.
  - 폴더는 선택만 되고 루트(activeFolderId=null) 복귀 UI가 없었음.
- **해결방향 (합의: 전부 진행)**
  - (A) 학습모드 = 세트 구성 화면. **범위(scope) × 순서(order) 2축** + 폴더 범위 시 **학습화면 자체 폴더 드롭다운** + 실시간 **요약/장수** + 0장 비활성.
  - (B) 학습모드 진입은 항상 setup을 보여주고, 진행 중 세션이 있으면 **「이어서 학습」** 버튼 노출. 「학습시작」 시 진행 세션 있으면 **덮어쓰기 확인**.
  - (C) 카드제작 초안 유지: 이동 후 돌아오면 이어쓰기. 저장/명시적 「새 카드」 시 초기화.
  - (D) 카드관리 **빵부스러기**(전체 ▸ 폴더 ▸ 하위) + 「폴더」 제목 클릭 → 루트.
- **구체적 수정** (DONE)
  - `js/domain/queries.js`: `buildStudyQueue({scope,order,folderId,selectedIds})`로 시그니처 변경. scope=folder인데 folderId 없으면 `[]`(전체로 새지 않음). order: created(저장순)/low-rounds/(random은 호출부 셔플).
  - `js/domain/migrate.js`: `ui.studyConfig{scope,order,folderId}`, `ui.createDraft` 기본값 추가(기존 데이터엔 머지로 안전 주입).
  - `index.html`: 학습 setup을 `#studySetup`(범위/폴더/순서/유사도 + `#studySummary` + `#startStudyBtn` + `#resumeStudyBtn`)과 `#studyPlayBar`(메타/회독/이전·다음)로 분리. 카드관리 폴더 헤더에 `#folderBreadcrumb` + 「폴더」 제목 `data-action="select-root-folder"`. 학습 중 수정 패널 `#studyEditPanel` id 부여.
  - `js/ui/study.js`: `renderStudySetup()` — 설정 복원·폴더옵션·요약/장수·시작 활성·이어서학습 표기.
  - `js/ui/manage.js`: `renderFolderBreadcrumb()`(`renderFolderTree`에서 호출).
  - `js/app/actions.js`: `readStudyConfig/onStudyConfigChange/startStudy(재작성: 확인 포함)/resumeStudy/goCreate`. `startNewCard`(초안 있으면 확인)/`editCard`(초안 우선)/`deleteCurrentCard`(새카드면 초안 비우기)/`saveCard`(저장 시 초안 null). `saveStudySession`은 config 저장, `restoreStudySession`에서 studyMode 의존 제거. 로그인/로그아웃 시 세션·초안·런타임 학습상태 정리(사용자 간 누수 방지).
  - `js/ui/router.js`: 카드제작 떠날 때 `saveCreateDraftOnLeave()`(내용 있으면 보존). manage/create 진입 시 studySession 자동삭제 **제거**(이어서학습 위해). `name==='study'`면 `renderStudySetup()`.
  - `js/main.js`: `nav-create→goCreate`(이어쓰기), `resume-study`/`select-root-folder` 매핑, 학습 설정 select 변경 → `onStudyConfigChange`, create 라우트 초안 우선 복원.
  - `css/layout.css`·`components.css`: setup 바/요약/빵부스러기 스타일, setup↔play 배타 표시(`#studySetup`/`#studyPlayBar`/`.study-workspace`/`#studyEditPanel`).
  - "새로고침 패치 효과란?": **메뉴 클릭 시 목록만 다시 그림**. 이번에 학습/제작 화면도 진입 시 결정적 상태로 렌더되도록 보강.
  - 검증: 자동화 브라우저(사용자 데이터와 분리)로 로그인→학습 setup(범위→폴더 전환 시 폴더 드롭다운 동적 노출, 0장 시 비활성)→빵부스러기 「전체」 렌더 확인. 모듈 로드 오류 없음.

---

## 9. 학습시작 후 작업영역 사라짐(치명) + 범위 보조선택 모달화 (추가5)

- **문제 (사용자 지적)**
  - (1) **치명**: 「학습 시작」을 눌러도 setup/플레이바 아래 학습 카드·채점 영역이 **아무것도 안 뜸**.
  - (2) 학습 setup이 조잡(폴더 드롭다운이 범위/순서 줄에 끼어듦). 폴더는 **모달**(카드관리 트리처럼)로 빼야 함.
  - (3) 플래그 범위는 **색을 직접 골라야** 함(단순 "플래그 있음"이 아니라 특정 색).
  - (4) "선택 카드"가 어디서 선택되는지 불명확.
- **원인**
  - (1) 8번에서 넣은 `#studySection .study-workspace { display:none }`이 **ID 특이성** 때문에 `body.study-playing .study-workspace { display:flex }`를 이김. → 학습 중(play)에도 작업영역이 계속 숨겨짐(렌더는 됐으나 비표시).
  - (2~4) 범위 보조선택이 인라인 드롭다운 하나뿐이라 폴더/플래그/선택카드를 구분해 다루지 못함.
- **해결방향**
  - (1) 숨김 규칙을 `body:not(.study-playing) #studySection …`로 바꿔, 학습 중엔 기본 표시값(flex/block) 사용 → 특이성 충돌 제거.
  - (2) 폴더 = **모달**(폴더 트리), (3) 플래그 = **색 스와치 7개**(`studyConfig.flag`), (4) 선택카드 = **카드 선택 모달**(체크리스트, `selectedIds` 공유). 모달은 `js/ui/pickers.js`로 모듈화.
- **구체적 수정** (DONE)
  - `css/layout.css`: setup↔play 표시 규칙을 `:not(.study-playing)` 가드로 재작성(작업영역·`#studyEditPanel`은 학습 중일 때만 표시, 특이성 안전).
  - `js/ui/pickers.js`(신규): `openStudyFolderPicker`(폴더 트리·하위 포함 카드수), `openStudyCardPicker`(전체선택/해제·실시간 카운트). 모달은 자체 리스너로 처리하고 backdrop 클릭 시 닫힘(전역 위임과 분리).
  - `index.html`: 인라인 `#studyFolderField/#studyFolder` 제거, `#studyScopeDetail`(범위별 보조 UI) 추가. 범위/순서는 한 줄 유지.
  - `js/ui/study.js`: `renderStudySetup` 재작성 — `scopeDetailHtml`(폴더=모달 버튼/플래그=색 스와치/선택카드=모달 버튼), `scopeText`(요약 문구). 미선택 경고(폴더·플래그·선택카드).
  - `js/app/actions.js`: `readStudyConfig`가 folderId/flag 보존(모달·스와치로 따로 지정). `pickStudyFlag/openStudyFolderModal/openStudyCardModal` 추가.
  - `js/domain/queries.js`: `buildStudyQueue`에 `flag` 인자 추가, scope=flag는 `flagColor === flag` 정확 매칭(미선택 시 `[]`).
  - `js/domain/migrate.js`: `studyConfig.flag` 기본값(1) 추가(기존 데이터 머지).
  - `js/main.js`: `pick-study-folder/pick-study-cards/pick-study-flag` 매핑, 설정 select 변경 대상에서 `studyFolder` 제거.
  - 검증: 자동화 브라우저로 카드 1장 생성→「학습 시작(1장)」→`#/study/play/0`에서 회독·빈칸 입력칸·전체 채점·학습 중 수정 패널까지 **작업영역 정상 표시** 확인. 플래그 범위 시 색 스와치 7개, 폴더 범위 시 모달 버튼 렌더 확인. 린트 0.

---

## 10. 이어서 학습 진행 복원 + 내보내기/미리보기/사이드바 (추가6)

- **문제 (사용자 지적)**
  - (1) 「이어서 학습」이 카드 **인덱스만** 복원하고, **빈칸 입력·채점 상태는 초기화**됨 → 1번부터 뭉치만 다시 하는 느낌.
  - (2) 폴더 내보내기는 카드관리에서 폴더를 미리 선택해야 해서, **모달로 고르는 UX**가 맞음.
  - (3) 사이드바 「학습 시작」이 진행 중 세션·이어학습과 **맥락 연계 없음**.
  - (4) 폴더 모달에서 **층위가 호버 전엔 안 보임** — 박스 테두리로 항상 드러나야 함.
  - (5) 학습 setup에서 범위 선택 후 **포함 카드 목록**을 요약 아래에서 볼 수 있어야 함.
- **원인**
  - (1) `saveStudySession`이 `cardIds/index`만 저장, `restoreStudySession`·`renderStudyCard`가 매번 blankStatuses를 새로 생성.
  - (2) `handleExport('folder')`가 `store.activeFolderId`(카드관리 현재 폴더)에 의존.
- **해결방향**
  - (1) `ui.studySession.cardProgress[cardId]`에 카드별 `{ blankStatuses, attemptRecorded, focus }` 저장. 채점·이전/다음·화면 이탈·빈칸 입력(디바운스) 시 스냅샷.
  - (2) `openExportFolderPicker` — 학습용 `openFolderPicker` 공용 모듈.
  - (3) 사이드바 메뉴에 **이어서 학습** 버튼(진행 세션 있을 때만, 카드명 포함).
  - (4) `.pick-folder-row`에 border/background 항상 적용, `.pick-folder-children` 왼쪽 실선.
  - (5) `#studyPreviewList` — `buildStudyQueue` 결과 카드 리스트 렌더.
- **구체적 수정** (DONE)
  - `js/services/study-session.js`(신규): `snapshotCurrentCardProgress`, `saveStudySession({resetProgress})`, `loadCardProgress`.
  - `js/app/actions.js`: 새 학습 시작 시 `resetProgress:true`. 채점·prev/next·hideAnswers에서 세션 저장. `handleExport('folder')`→모달.
  - `js/ui/router.js`: study-play 이탈 시 `saveStudySession()`.
  - `js/ui/study.js`: `loadCardProgress`로 카드 전환 시 입력·채점 복원. setup에 `#studyPreviewList`.
  - `js/ui/pickers.js`: `openFolderPicker` 공용화, `openExportFolderPicker` 추가, 폴더 행 박스 스타일 클래스.
  - `js/ui/sidebar.js`: `#sidebarResumeBtn` 맥락 표시.
  - `index.html`, `css/components.css`: resume 버튼·preview list·pick-folder 박스 스타일.
  - `js/main.js`: 빈칸 입력 500ms 디바운스 세션 저장.
  - 내보내기/가져오기: 전체·유저·폴더(모달)·병합/덮어쓰기 가져오기 — 기존 `import-export.js` 로직 유지, 폴더 UX만 개선. (실사용 검증은 사용자 확인 예정)

---

## 11. 목차 관리(자동 인식→저장) + 목차 기반 빈칸 (추가7)

- **문제 (사용자 지적)**
  - 「← 왼쪽에 반영」을 눌러도 왼쪽 제목 input이 비어 보임.
  - 카드제작 화면의 목차 미리보기가 볼품없고, `외 N개…` 표시가 거슬림.
  - 목차 항목으로 빈칸 만들기/해제 흐름이 불안정하고, 더보기 상태가 작업 후 풀림.
- **원인**
  - (1) 전역 `input{width:100%}`가 flex row 내부에서 `.outline-tree-title` 너비를 비정상적으로 줄임(약 25px) → 텍스트가 “안 보이는 것처럼” 보임.
  - (2) 자동 인식 미리보기 → 저장본 반영 시 `syncWorkingFromDom`이 빈 값을 덮어쓸 수 있고, `innerHTML` 기반 `value` 속성은 케이스에 따라 불안정.
  - (3) 미리보기는 단순 줄 나열 + “외 N개…”로 끝, 액션(목차 기반 빈칸)과 연결이 약함.
- **해결방향**
  - (A) 모달 왼쪽 제목 input은 전역 width 규칙 영향 제거 + flex 안정화.
  - (B) 모달 렌더 후 state → `input.value` hydration으로 값 주입(HTML attribute 의존 제거), adopt는 preview를 그대로 복사.
  - (C) 카드제작 목차 미리보기를 “실제 목차”처럼 박스화 + 더보기/접기 + 목차 기반 빈칸 액션 제공.
- **구체적 수정** (DONE)
  - `js/ui/outline-modal.js`: adopt 시 preview→working 복사, 렌더 후 `hydrateWorkingFromState`로 input 채움.
  - `css/components.css`: `.outline-tree-title` flex 안정화 및 전역 `input{width:100%}` 영향 제외.
  - `js/ui/create.js`: 저장된 목차 미리보기 패널(트리/행번호) + 더보기/접기 상태(`ui.outlineSummaryExpanded`) 유지. “모두 빈칸으로/선택 빈칸 만들기/모두 해제”.
  - `js/app/actions.js`: 목차 저장/목차 빈칸 적용·해제 시 **자동 저장(silent)** 트리거.
  - `js/domain/outline.js`: `listOutlineTokens` 추가(중복/disabled 처리 통합), 목차 기반 후보 생성 안정화.

---

## 12. 자동 빈칸 UX 정리 + 취소 동작/단축키 (추가8)

- **문제**
  - 「추천 5개」가 누르자마자 적용되어 실수 위험. 사용자는 “적용할까요?” 확인만 원함.
  - 「빈칸 후보 고르기」 모달에서 취소가 안 먹음.
  - Ctrl+B를 드래그 없이 써도 커서 주변 단어를 빈칸으로 만들고 싶고, 조사 처리도 필요.
  - Ctrl+S 저장 필요.
- **원인**
  - `close-modal` 액션이 전역 클릭 위임 map에 없어서 취소 버튼이 무시됨.
  - 커서 기반 자동 선택/조사 분리에서 “표면 텍스트”가 사라지는 케이스가 존재.
- **해결방향**
  - 추천 5개는 confirm 1회로만, 후보 모달은 “수동 선택” 용도로 유지.
  - close-modal은 전역 map으로 처리해 어떤 모달이든 닫힘 일관화.
  - Ctrl+B: 드래그면 사용자가 선택한 범위를 존중, 커서만이면 단어 자동 선택 + 조사 분리(조사는 칩 뒤에 남김).
  - Ctrl+S: 브라우저 저장 대신 카드 저장.
- **구체적 수정** (DONE)
  - `js/app/actions.js`: `confirmQuickAutoBlank` 추가(추천 N개 confirm).
  - `js/ui/modal.js` + `js/main.js`: `close-modal` 매핑 추가, backdrop `data-action` 제거.
  - `js/ui/chip-editor.js`: 커서 기반 단어 자동 선택 + 조사 분리(표면 유지). 드래그 선택은 조사 포함 허용.
  - `js/main.js`: Ctrl+S 저장 훅 추가.

---

## 13. 카드제작 저장 UX + 사이드바/레이아웃 (추가9)

- **문제**
  - 이동 시 “무조건” confirm이 뜨면 과함. 실제 변경이 있을 때만 안내 필요.
  - 5분 자동 저장 + 마지막 저장 시각 표시 필요.
  - 사이드바 높이/스크롤이 본문 길이에 따라 불안정(짧을 때 잘린 듯/길 때 제한).
  - 사이드바 단축키 안내 박스 제거 요청.
- **해결방향**
  - 마지막 저장 스냅샷과 현재 폼 스냅샷 비교해 dirty일 때만 confirm.
  - 카드제작 상단에 “최근 저장(자동 또는 수동 중 최신)” 시간 표시.
  - 사이드바는 “본문이 짧으면 사이드바만 스크롤” 조건을 JS로 동기화(형제 높이 참조 필요).
- **구체적 수정** (DONE)
  - `js/domain/migrate.js`: `ui.createSavedSnapshot`, `ui.lastSavedAt`, `ui.lastAutoSaveAt` 기본값 추가.
  - `js/ui/router.js`: 섹션 이동 시 dirty 비교 후에만 confirm.
  - `js/main.js`: 5분마다 `autoSaveCreate`, Ctrl+S 저장, 사이드바 높이 동기화 호출.
  - `js/ui/render.js` + `js/app/actions.js`: 저장 시각 평문 표시(`#createSaveStamp`).
  - `index.html`: 사이드바 단축키 박스 제거, 목차 생성·관리 버튼을 목차 패널 헤더로 이동.
  - `css/layout.css`: 사이드바 하단 여백 추가(“잘린 느낌” 완화).
