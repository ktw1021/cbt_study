# 개선 기록 — 2026-06-15

테스터 피드백 기반. 형식: 문제 / 원인 / 해결방향 / 구체적 수정.

---

## 1. 학습 중 입력이 날아감 (최우선)

- **문제**
  - 학습 중 빈칸 「빈칸1」 버튼을 누르면 다른 칸에 입력해 둔 글이 사라짐.
  - 채점 후, 미채점 칸 여러 개에 써 두고 Enter → 커서 있던 칸만 채점되고 나머지 입력이 사라짐.
- **원인**
  - 입력값의 단일 출처가 없음. DOM(`input.blank-field` value)과 메모리(`currentBlankStatuses[].user`)로 분산.
  - `currentBlankStatuses[].user`는 **채점할 때만** 채워짐.
  - `focusBlankUI`/`gradeBlankOnEnter`가 `refreshStudyViews()`로 해설 패널 전체를 innerHTML 재생성 → 미채점 칸은 빈 값으로 다시 그려져 DOM 입력 증발.
- **해결방향**
  - 재렌더 전에 DOM 값을 모델로 흡수(draft 동기화). 포커스 이동은 재생성 없이 patch만.
- **구체적 수정** (DONE)
  - `js/ui/study.js`: `syncDraftFromDOM()` 추가(미채점 칸 DOM value → `status.user`).
  - `focusBlankUI()`: `refreshStudyViews()` 호출 제거 → `syncDraftFromDOM()` + `paintInlineBlanks()`(클래스/포커스만).
  - `refreshStudyViews()`: 재생성 직전 `syncDraftFromDOM()` 호출.
  - `js/app/actions.js` `gradeBlankOnEnter()`: 시작 시 `syncDraftFromDOM()` → 다른 칸 입력 보존.

---

## 2. 앱 상태 되돌리기/다시하기 확인

- **문제**
  - 에디터 내 평범한 Ctrl+Z(글자 단위)는 OK. 그러나 앱 상태 단위 되돌리기는 사용자가 의도/범위를 모른 채 실행될 수 있음.
- **원인**
  - `Ctrl+Alt+Z`/사이드바 「되돌리기」가 확인 없이 앱 전체 상태 스냅샷을 즉시 복원.
- **해결방향**
  - 앱 상태 되돌리기 실행 전 확인 알림으로 사용자 동의를 받음(글자 단위 Ctrl+Z와 구분 안내).
- **구체적 수정** (DONE)
  - `js/services/undo.js`: `hasUndo()` 추가.
  - `js/app/actions.js` `undoAppState()`: `confirm()`으로 확인 후 실행, 빈 스택 안내.

---

## 3. 데이터 안전(GitHub Pages 배포 대비)

- **문제**
  - 배포·리팩터링 중 저장 키가 바뀌면 기존 사용자 IndexedDB 데이터 유실 위험.
- **원인**
  - DB 이름/스토어/키가 코드 상수라 무심코 변경 가능.
- **해결방향**
  - 키를 불변으로 고정하고 변경 금지 의도를 코드에 명시. 스키마 변경은 migration으로만.
- **구체적 수정** (DONE)
  - `js/config.js`: `DB_NAME`/`DB_STORE`/`STATE_KEY` 변경 금지 주석.

---

## 4. 플래그 지정 UI가 색이 아닌 텍스트

- **문제**
  - 카드 제작에서 플래그를 「1 빨강」 같은 텍스트 `select`로만 골라 실제 색이 안 보임.
- **원인**
  - `#cardFlag`가 네이티브 `<select>`라 옵션에 색 스와치를 못 그림.
- **해결방향**
  - 실제 플래그 색(`.flag-0~7`)을 보여주는 스와치 버튼 그룹으로 교체. 값은 hidden input으로 유지해 기존 읽기/저장 로직 호환.
- **구체적 수정** (DONE)
  - `index.html`: `#cardFlag`를 hidden input + `#cardFlagPicker`(색 버튼 8개)로 교체.
  - `css/components.css`: `.flag-picker`/`.flag-pick`/`.active` 스타일 추가.
  - `js/ui/create.js`: `syncCardFlagPicker()` 추가, `renderCreateForm`에서 호출.
  - `js/app/actions.js`: `pickCardFlag()` 추가. `js/main.js`: `pick-flag` 액션 연결.

---

## 5. 죽은 「샘플 데이터 삭제」 버튼

- **문제**
  - 카드관리 상단 「샘플 데이터 삭제」 버튼이 아무 기능도 안 함.
- **원인**
  - 샘플 카드를 주입하는 코드가 전혀 없음(모든 카드 `isSample:false`). `deleteSampleData()`는 항상 "삭제할 샘플 없음"만 반환 → 죽은 버튼.
- **해결방향**
  - 버튼과 관련 코드 제거. (`isSample` 필드는 데이터 호환 위해 스키마에 그대로 둠)
- **구체적 수정** (DONE)
  - `index.html`: 「샘플 데이터」 stat/버튼 제거.
  - `js/main.js`: `delete-sample` 액션 매핑 제거.
  - `js/app/actions.js`: `deleteSampleData()` 함수 제거.

---

## 6. 빈칸 토큰 노출/취약성

- **문제**
  - 카드 제작·학습수정 해설 입력칸(textarea)에 `[[BLANK1]]` 날것 토큰이 그대로 노출 → 사용자가 직접 치거나 일부를 지우면 파싱이 깨짐.
- **원인**
  - 편집 표면(textarea)이 토큰 문자열을 그대로 다루고, `writeTextarea`의 `el.value=` 통짜 덮어쓰기로 native undo도 파괴.
- **해결방향**
  - 빈칸을 편집 불가한 "칩"으로 표시하는 contenteditable 칩 에디터로 전환. 화면에 토큰을 노출하지 않음. 저장 포맷(`explanationText`= ...[[BLANK1]]... + `blanks[]`)은 그대로 유지해 기존 카드 무손상(읽을 때 토큰→칩, 저장 시 칩→토큰).
- **구체적 수정** (DONE)
  - `js/ui/chip-editor.js` 신설: 토큰↔칩 parse/serialize, Ctrl+B(선택→칩), 칩 ×제거, 정답 동기화(`chipSetAnswer`), 자동빈칸(`chipApplyTokens`), Enter/붙여넣기 평문화.
  - `index.html`: 해설 textarea 2개(`explanationTemplate`, `studyEditExplanation`)를 `contenteditable` 칩 에디터로 교체.
  - `css/components.css`: `.chip-editor`/`.cz-chip`/× 스타일.
  - `js/ui/create.js`: 칩 에디터 기반으로 렌더/읽기/미리보기/정답슬롯 재작성, 슬롯 입력은 칩만 갱신해 포커스 유지.
  - `js/app/actions.js`: make/remove/jump/auto 빈칸을 칩 API로 재작성. 저장 시 빈 정답 검증 추가(→ 8번도 해소).
  - `js/main.js`: contenteditable용 Ctrl+B, input 라우팅, 칩 × 클릭 처리.
- **추가 (DONE)**
  - 칩 에디터 자체 되돌리기/다시하기: `chip-editor.js`에 편집 스냅샷 히스토리(최대 100단계). **Ctrl+Z**(되돌리기) / **Ctrl+Shift+Z·Ctrl+Y**(다시하기). 글자 입력·빈칸 만들기(Ctrl+B)·해제·자동빈칸 모두 한 타임라인으로 되돌림. (앱 데이터 되돌리기 Ctrl+Alt+Z와는 별개)

---

## 7. 입력 전 정답 호버 엿보기

- **문제**
  - 답을 모를 때, 입력 전 빈칸에 1초 호버하면 정답이 잠깐 보였다 사라지는 학습 보조가 필요.
- **원인**
  - 현재 정답은 채점 후에만 노출.
- **해결방향**
  - 미채점 빈칸 hover 1초 → 정답 잠깐(약 1.1초) 표시 → 해제/시간초과 시 숨김.
- **구체적 수정** (DONE)
  - `js/ui/study.js`: `handleBlankPeekOver/Out` + 플로팅 툴팁. 채점된 칸은 제외.
  - `js/main.js`: `mouseover/mouseout` 위임 연결.
  - `css/components.css`: `.blank-peek` 스타일.

---

## 8. 자동빈칸 정답 누락

- **문제**
  - 자동빈칸 적용 시 토큰만 생기고 정답이 비어 채점이 깨지는 경우.
- **원인**
  - DOM 슬롯/blanks order 동기화 타이밍, 빈 정답 검증 부재.
- **해결방향**
  - 칩 생성 시 정답(=선택/추출 단어)을 항상 함께 넣고, 저장 시 빈 정답을 막는다.
- **구체적 수정** (DONE)
  - 칩은 항상 answer를 보유(`chipApplyTokens`/`chipMakeBlank`). `saveCard`/`saveStudyEdits`에서 빈 정답 검증 후 저장 차단.

- 호버 표시 지속 시간은 이후 0.5초로 조정(요청). 호버 지연은 1초 유지.

---

## 9. 사이드바 토글 첫 클릭 먹통

- **문제**
  - 접힌 상태에서 펼치기를 눌렀을 때 한 번은 동작하지 않음(두 번째 클릭에야 펼쳐짐).
- **원인**
  - 학습 풀이 중 자동 접힘은 `body`에 클래스만 붙이고 저장값(`ui.sidebarCollapsed`)은 안 바꿈 → 화면(접힘)과 저장값(펼침)이 어긋남. `toggleSidebar`가 저장값 기준으로 다음 상태를 계산해 첫 클릭이 같은 상태로 설정됨.
- **해결방향**
  - 토글 다음 상태를 "현재 화면(body 클래스)" 기준으로 계산.
- **구체적 수정** (DONE)
  - `js/ui/router.js` `toggleSidebar`: `!document.body.classList.contains('sidebar-collapsed')`로 계산 후 저장값 동기화.

---

## 10. 카드관리 상세 — 버튼이 스크롤 맨 아래에 묻힘

- **문제**
  - 카드 상세에서 「수정하기/바로 학습/삭제」를 누르려면 매번 맨 아래까지 스크롤해야 함.
- **원인**
  - `#manageDetail` 박스 전체가 스크롤 영역이고 버튼도 그 안 마지막에 위치.
- **해결방향**
  - 본문(`.detail-body`)만 스크롤, 액션 버튼(`.detail-actions`)은 하단 고정 푸터로 분리.
- **구체적 수정** (DONE)
  - `index.html`: `#manageDetail`을 flex 컬럼 컨테이너로 변경.
  - `js/ui/manage.js`: 본문/액션 분리 마크업.
  - `css/components.css`: `.manage-detail`/`.detail-body`/`.detail-actions` 스타일.

---

## 11. 빈칸 "정답"을 자유롭게 고칠 수 있던 문제

- **문제**
  - 해설에서 드래그해 만든 빈칸의 정답을 입력칸에서 다시 마음대로 고칠 수 있어, 본문 단어와 정답이 어긋날 수 있었음.
- **원인**
  - 빈칸별 정답이 편집 가능한 `textarea`였고, 동의어(`||`/줄바꿈) 입력까지 열어둠.
- **해결방향**
  - 동의어 사전은 만들지 않는다(혼자 잠깐 보는 용도). 정답 = 드래그한 단어로 고정, 편집은 막는다. 띄어쓰기·사소한 오타는 채점기가 흡수.
- **확인/구체적 수정** (DONE)
  - 채점(`js/services/grading.js`)은 `normalizeTight`로 공백 제거 후 비교 → "권리 능력 = 권리능력" 등 띄어쓰기 차이 이미 정답 처리. 추가 작업 불필요.
  - `js/ui/create.js`: 제작·학습중수정의 빈칸 정답 `textarea`를 읽기 전용 표시(`.answer-readonly`)로 교체. `onAnswerSlotInput`/`chipSetAnswer` 사용 제거.
  - `js/main.js`: 정답 슬롯 input 라우팅(`[data-answer-key]`/`[data-study-answer-key]`) 제거.
  - `css/components.css`: `.answer-readonly` 스타일 추가.

---

## 12. 학습 중 메모칸에서 Enter → 전체 채점(치명적)

- **문제**
  - 학습 화면의 메모/수정 `textarea`에서 Enter를 치면 줄바꿈이 아니라 "전체 채점"이 실행됨.
- **원인**
  - `js/main.js` keydown에서 학습 섹션의 모든 `textarea` Enter를 `gradeCurrent()`로 연결.
- **해결방향**
  - 해당 동작 제거. 채점은 빈칸칸 Enter 또는 "전체 채점" 버튼으로만.
- **구체적 수정** (DONE)
  - `js/main.js`: 학습 textarea Enter→채점 블록 삭제(이제 메모 Enter는 줄바꿈). 미사용 `isText` 변수도 정리.

---

## 13. 카드 편집 시 학습 기록 초기화 (확인 결과: 정상)

- **문제 제기**
  - 카드를 편집하면 이전 학습 기록(회독수/오답수)이 사라지는 것 아니냐.
- **확인**
  - `saveCard`(카드관리)·`saveStudyEdits`(학습 중 수정) 모두 카드 단위 통계(`rounds`/`wrongCount`/`lastResult`)는 건드리지 않고 보존함.
  - 빈칸 단위 입력(`lastInput`/`lastResult`)만 새 빈칸으로 교체되며 초기화 — 다음 회독은 어차피 처음부터 푸는 게 맞으므로 의도대로.
- **결론** (변경 없음)
  - 코드가 이미 의도와 일치. 추가 수정하지 않음.
