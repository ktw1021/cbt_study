# 패치 노트 — 2026-09-04

홈·폴더 공유·계정 이전에 이어, 긴 빈칸·학습 UX를 다듬고 **알파 테스터 코드**로 초대받은 분만 들어올 수 있게 했습니다.
(개발 상세는 `dev-notes-2026-09-04.md`와 한 쌍)

---

## 1. 「CBT 빈칸 학습」을 누르면 홈으로 갑니다

- 왼쪽 위(좁은 화면은 상단) **타이틀이 홈 버튼**입니다. 누르면 **카드관리**로 갑니다.
- 학습 중에 눌러도 이어서 학습은 그대로 남아 있습니다.

---

## 2. 폴더를 파일로 공유할 수 있습니다

- **폴더 내보내기** = 공유. 고른 폴더와 그 안의 카드만. 미분류는 안 들어갑니다.
- 가져오면 **내 폴더 목록 맨 위**에 붙습니다. 이름 겹치면 **「형법 (1)」**.
- 제목·해설·빈칸이 같은 카드가 있으면 중복 목록을 보여 주고, 건너뛸지 받을지 고릅니다.
- 받는 쪽 회독·오답은 **0**. 카드에 **최초 제작자 이름**이 남습니다.

---

## 3. 전체·유저 내보내기 -> 가져오기 = 계정 추가/이동을 지원하려는 의도

- 새 계정으로 추가해줌. 이름 겹치면 **「철수 (1)」**.
- 비밀번호는 가져온 뒤 바로 정합니다.
- **이미 들어가 있던 계정은 그대로**되도록 방어.
- 이 앱 파일이 아닌 JSON은 거절합니다.

---

## 4. 배포 후에 강한 새로고침을 안 해도 됩니다

- 새 버전이 있으면 **한 번만** 다시 불러옵니다.
- 이 패치를 **처음 받은 뒤 한 번은** 예전처럼 새로고침이 필요할 수 있습니다.

---

## 5. 알파 테스터 코드로 입장합니다

- 앱을 열면 **알파 테스터 코드**를 먼저 묻습니다. (기존 이름+비밀번호 로그인보다 앞)
- 맞는 코드를 한 번 넣으면 **이 브라우저에 저장**되어, 다음에 다시 묻지 않습니다.
- 운영 쪽에서 코드를 바꾸면 예전에 저장해 둔 값은 더 이상 통하지 않고, **새 코드**를 다시 입력해야 합니다.

---

## 6. 긴 빈칸·힌트가 해설 칸을 밀지 않습니다

- 카드제작에서 문장처럼 긴 빈칸을 등록해도 **해설 칸이 가로로 미친 듯이 안 늘어납니다.**
- 학습에서도 ○○○ 길이를 보면서 글자를 맞추는 느낌은 유지하고, 줄이 길면 **해설 본문과 같이** 다음 줄로 이어집니다.
- 호버로 정답을 엿볼 때, 여러 줄 빈칸이면 **첫 줄 바로 위**에 뜹니다. (빈칸윗줄을 가리지 않음)
- 힌트를 켠 채로 그 칸에 타이핑할 때, ○○○가 줄어들며 힌트가 **잠깐 사라졌다 다시 뜨던** 깜빡임을 줄였습니다.
- 채점 후 아래에 보이는 안내 문구는 **「정답: …」** 형식입니다.

---

## 7. 학습 시작·범위를 고르기 쉬워졌습니다

- 학습 세트에서 **「선택 카드」 범위 옵션은 없애고,** 대신 미리보기 목록에서 **체크**로 넣을 카드를 고릅니다. (전체 선택/해제)
- **폴더 여러 개**를 골라 한꺼번에 학습할 수 있습니다. (조상·자손 겹치면 정리됨)
- 폴더·범위만 바꿀 때는 미리보기가 **다시 전체 선택**됩니다. 같은 목록에서 체크만 바꿀 때는 **스크롤이 유지**됩니다.
- 미리보기 각 줄에서 **수정 / 학습**으로 바로 갈 수 있습니다.
- 카드관리 폴더 경로 옆에 **학습** 버튼이 있습니다. (그 폴더에 카드가 있을 때)

---

## 8. 학습 중 조작

- 학습 화면 상단에 **수정**이 있어, 지금 보는 카드를 바로 고칠 수 있습니다.
- **다시 풀기**하면 입력·채점이 깨끗이 초기화됩니다.
- 빈칸에서 **다른 칸으로 이동**하거나 포커스를 벗어나면, 비어 있지 않은 칸은 채점됩니다. (Enter도 그대로)

---

## 9. 카드제작

- **새 카드**를 누를 때, 작성 중이던 내용이 있으면 확인할지 묻습니다.
- **삭제**는 이미 저장된 카드일 때만 쓸 수 있습니다. (작성 취소를 삭제로 오해하지 않게)
- 넓은 화면에서는 스크롤하면 제작 머리 영역이 따라옵니다. 
- 좁은 화면의 상단바에 있던 관리/제작/학습 버튼은 왼쪽에 붙였고, **저장**을 추가하여 따라오게 했습니다.

---

## 10. 기타 수정

- 학습 중 카드 수정의 클릭을 가로채던 투명영역의 문제를 고쳤습니다.
