# 패치 노트 — 2026-09-10

채점은 **맞았는지**와 **어디가 틀렸는지**를 나누고, 학습·제작에서 길거나 지저분한 빈칸이 화면을 망가뜨리던 점을 고쳤습니다.
(개발 상세는 `dev-notes-2026-09-10.md`와 한 쌍)

---

## 1. 채점 — 통과는 비슷하면, 표시는 글자 그대로

- **빈칸을 맞았다고 볼지**는 예전처럼 유사도(설정한 % 이상)입니다. 글자를 **더 쓰면** 통과하지 않습니다.
- **어디에 뭐가 틀렸는지**는 유사도로 칠하지 않습니다. 앞에서부터 글자·띄어쓰기를 대조합니다.
  - 맞은 글자: 초록 배경
  - 다른 글자: 빨간 배경
  - **추가로 쓴 글자**: 빨간 배경 + 취소선
  - **띄어쓰기를 빼먹음**: ∧
  - **띄어쓰기를 잘못 넣음**: 찡긋(‿)
- `9000` / `9,000`처럼 **천 단위 콤마만 빠진 것**은 띄어쓰기 오류로 보지 않습니다. 뒤 문장이 밀리지도 않습니다.
- 글자를 빼먹거나(`소장부본` → `소장부`) 군더더기를 넣어도, 그 뒤가 다시 맞으면 **나머지를 통째로 오답 처리하지 않습니다.**

---

## 2. 학습 화면

- 채점 후 **「정답: …」** 은 빈칸 **아래**에 붙고, 빈칸 뒤 본문(「입니다.」 등)은 **같은 줄**에 남습니다. 여러 줄 빈칸에서도 문장 한가운데로 밀려 가지 않습니다.
- 빈칸에서 **Enter로 채점해도 커서는 그 자리**에 남습니다.
- 문제칸 **가리기**: 헤더의 가리기 → 세로(좁은 화면은 가로) 탭. 다시 누르면 펼칩니다. 정답을 문제칸에 적어 두고 시험처럼 풀 때 카드를 고치지 않아도 됩니다.

---

## 3. 붙여넣기·정답 문단

- 윈도우에서 붙여넣을 때 줄이 두 번 접히거나, 빈 줄에 커서가 안 잡히던 문제를 고쳤습니다. (`Enter` 줄바꿈을 한 종류로 맞춤)
- 정답의 **줄바꿈은 한 문단**, 다른 표현만 `||` 로 가릅니다. `1.` / `2.` 가 이어지면 동의어로 쪼개지 않습니다.
- 목차 **모두 빈칸으로**가 숫자 항목(`1.`, `2.`)을 빼먹지 않습니다.
- 빈칸을 풀었다가 **다시 드래그해서 묶을 수** 있습니다. 안 되면 이유를 알려 줍니다.

---

## 4. 카드제작

- 주기적 **임시저장**은 초안만 남깁니다. **저장**을 누르지 않으면 카드 목록에 확정되지 않습니다. (표시도 「임시저장」)
- 빈칸 칩을 지워도 미리보기·정답 칸이 따라오도록 고쳤습니다.
- 목차 적용 등 뒤에서 돌아가는 저장은, 문제/해설이 비어 있을 때 경고창을 띄우지 않습니다.
