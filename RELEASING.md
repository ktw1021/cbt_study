# 개발·검수·배포

`ktw` → `staging` → `master`

| 브랜치 | 용도 | 반영 기준 |
|---|---|---|
| `ktw` | 기능 개발·수정 | 관련 회귀 검사 후 목적별 커밋·푸시 |
| `staging` | 테스트 서버 | `ktw` 변경을 합쳐 실제 화면·저장·업데이트 확인 |
| `master` | 정식 배포 | staging에서 확인한 변경을 검토 후 반영 |

## 릴리스 순서

1. 이전 배포 커밋과 전체 diff를 검토하고 [회귀 검사](tests/README.md)를 실행한다.
2. 날짜별 `dev-notes-*.md`에는 원인·수정·검증·남은 한계, `patch-notes-*.md`에는 사용자가 체감하는 변화를 간단히 쓴다.
3. `js/ui/patch-notes.js` 목록 맨 위에 새 파일을 넣고 `version.json`의 `v`를 갱신한다. 같은 날 재배포는 `2026-09-16.1`처럼 구별한다.
4. `ktw`를 커밋·푸시하고 staging에 반영한다. 검수한 커밋과 결과를 기록한 뒤 master로 반영한다. master에 올리는 것과 테스트 서버 배포는 각각 명시적으로 실행한다.

## 테스트 서버 주의점

- **브랜치를 만드는 것만으로 테스트 서버가 생기지는 않는다.** staging 전용 배포 대상·URL·자동 배포 설정이 필요하다. 현재 저장소에는 staging 배포 설정이 없다.
- staging은 운영과 **다른 origin(프로토콜·호스트·포트)** 을 사용한다. 같은 호스트의 경로만 다르면 IndexedDB·로그인 데이터가 공유된다. 실제 계정 대신 테스트 데이터·별도 브라우저 프로필을 사용한다.
- `index.html`의 버전 확인과 `sw.js`의 리소스 갱신도 확인한다. 기본 localhost에서는 이 경로를 건너뛴다. `tests/release-browser.mjs`는 별도 루프백 호스트에서 확인하며, 실제 테스트 서버·운영 배포 확인을 대신하지 않는다.
- 빌드·런타임 라이브러리 추가는 없다. DB 키는 바꾸지 않고, 배포 문제를 브라우저 DB 삭제로 해결하지 않는다.
